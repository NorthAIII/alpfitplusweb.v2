# Phase 2 — Retrospektif ve Kalite Kontrol

← PHASE-2 · retrospektif-ve-kalite

> `_dev/phases/PHASE-2.md` → Retrospektif ve Kalite Kontrol Sonuçları'nın bölme çocuğudur (review-phase boyut kapısı, 2026-09-23; faz **hâlâ aktifken** bölündü — iki bölüm yazıldığında parent 15.657 → 24.073 token'a çıkarak kırmızı çizgiyi aştı, teşhis (a) gerçek büyüme). Parent'ta her iki bölümün **self-yeten özeti** ve bu dosyanın pointer'ı durur; **retrospektifin tam metni ve on eksenlik kalite kontrol tablosu buradadır.**
>
> Kanon bu iki bölümü faz dokümanının **tarihsel** rolü olarak sayar ve büyüdüğünde bölünmelerini söyler (CLAUDE.md → Boyut ve Bölünme → "Faz dokümanı hibrittir"); snapshot tabloları (Task Listesi, UAT özeti) bu yüzden parent'ta kaldı.

---

## Retrospektif

> `/devflow:review-phase` oturumunda dolduruldu (2026-09-23).

### Ne İyi Gitti?

- **Bulgu yapıyla kapandı ve yan ürün olarak bir yayın kapısı doğdu.** B-029 cümle cümle değil `CAPABILITIES` ile kapatıldı; altı tüketicinin altısı sabitten okuyor ve B-040'ın kanıt komutu 15 → 10 → **3 satıra** indi. Asıl kazanç kapsam dışıydı: bir kalemi `yolda`'dan `simdi`'ye taşımak, o kalemi *"henüz yok"* diye anan cümleleri derleme hatasına çeviriyor (`upcomingCapability`/`stageNote`) — faz yalnız bugünkü yanlışı düzeltmedi, aynı sınıfın sessizce geri gelmesini **yapısal olarak** imkânsız kıldı (UAT #4: batarya 216 → 154, iki rota 200 → 500).
- **Kapılar yazılırken sınandı, sonradan değil.** Yasal beyan kapısında 12 + 14 negatif kontrol koşuldu ve **ikisi kapının kendi fail-open'ını buldu** (göreli URL'li depo okuması · cast'li doğrudan izleyici çağrısı) — yani kapı doğduğu gün kör değildi. UAT'ta dokuz ters çevirme daha koşuldu, dokuzu da kırmızı verdi. Faz 1'in "yeşil kırmızıya dönebiliyor mu" pratiği bu fazda **kapı tasarımının içine** taşındı.
- **Devralınan iddia dört kez ölçümle çürütüldü ve üçü bir düzeltmeyi durdurdu.** TASK-2.09 bir cümlenin aslında doğru olduğunu buldu (*"yetkiler geri alınır"* — `revokeTemplate` üzerinden koşuyor), TASK-2.10 devralınan `nextVersion` devrini, TASK-2.13 bir alt metin iddiasını, TASK-2.01 ise döndürme ön koşulunu çürüttü (parmak izi eşleşmedi → TASK-2.03 iptal). Ölçmeden "düzeltmek" doğru cümleleri bozacaktı.
- **Devralınan kapsam daralmaları ölçülünce üçü de büyüdü — ve büyümüş hâl teslim edildi.** *"Beş karşılıksız iddia"* aslında ~124 cümlelik bir sınıftı (riskli alt küme tarandı: 27 küme / 66 dosya / **129 vuruş satırı**); *"üç yasal beyan"* en az sekiz olguydu (kapı **9 dal / 31 test** ile kapandı); *"iki anahtar döndürülecek"* ölçümle düştü. Faz dar tanımına sığınmadı.
- **Kullanıcı kararı fazın kilitlenmesini önledi ve ölçülen zemin kaybolmadı.** B-011'in kalan işi kullanıcının DNS adımına bağlıydı; ILKELER'in pazarlıksız maddesi uygulandı ve iş "Alan adı geçişi" fazına taşındı. Taşıma kayıpsız: Squarespace yönergesi, TXT/NS/SOA bozulmama tabanı, bugünkü başarısızlık biçimi (örtük MX) ve kapanış ölçümünün beş adımlık sırası bulgunun atomuna mezun edildi — o faz sıfırdan başlamayacak.
- **Faz dokümanı iki kez daha yaşarken bölündü** (`PHASE-2-ARASTIRMA` plan-phase'te, `PHASE-2-KAPSAM` verify'ın boyut kapısında). Dördü de kapanışta çizginin altında: parent 15.657 · ARASTIRMA 10.278 · UAT 10.249 · KAPSAM 6.555 token.

### Ne Kötü Gitti?

- **Ölçüm aracının kendisi üç kez kusurluydu ve üçünü de kontrol yakaladı, tasarım değil.** UAT'ta bir `sed` sessizce boş çıktı verip **sahte yeşil** üretti (duyarlılık kontrolü yakaladı); bir tarayıcı sondası **uydurma** bir hata koduyla kurulup 8/12 okudu (gerçek kod kümesiyle 16/16); TASK-2.12'nin tarama yöntemi sondayla düzeltildi (21 → 121 vuruş). Ortak ders tek cümle: *ölçüm kurmadan önce ölçtüğünün var olduğunu kanıtla.*
- **Bayat üretim konteyneri (3100) bu fazda da ölçüm turlarını bozdu — Faz 1 bunu yazmıştı ve öngörüsü doğrulandı.** `docker compose build` imajı tazeliyor ama konteyneri yeniden yaratmıyor; `perf.mjs` 3100'e çivili ve env ile yönlendirilemiyor. UAT üretim senaryolarını taze imajdan koşan geçici bir konteynere taşımak ve perf için iki yüzeyin **birebir aynı HTML** ürettiğini ayrıca kanıtlamak zorunda kaldı. B-019 hâlâ açık.
- **UAT iki tur koştu ve bulgu fazın kendi teslimi üzerindeydi.** Senaryo 26, TASK-2.07'nin açtığı onay e-postasının **doğrulanmamış bir adrese** gidebildiğini gösterdi: yeni bir dış gönderim yüzeyi açılırken *"alıcı kim ve kim doğruluyor"* sorusu task'ın kendi test kriterlerinde yoktu. Bedel bir düzeltme task'ı (TASK-2.21) + bir zorunlu yeniden koşum.
- **İkinci sayaç, birincisinin ödenmiş dersini tekrarlattı.** `confirmCapped` eklenince `validPayload()`'ın sabit varsayılan adresi mail kanalını açan yedi senaryoyu aynı kotaya koydu ve dosyanın ortasında sahte kırmızı doğdu. Aynı sınıf Faz 1'de IP için ödenmişti (*"her senaryo kendi IP'sini taşır"*); ikinci sayaç eklenirken hatırlanmadı. Memory atomu bu turda genelleştirildi.
- **Kapılar hâlâ kırmızıya dönemiyor** (B-030) — Faz 1'in aynı kalemi ikinci fazda da geçerli: beş kapının dördü eşik altında bile çıkış kodu 0 veriyor, yani bu fazın bütün "yeşil" ölçümleri yine **elle okundu**.

### Sonraki Faz İçin Öneriler

<!-- Alınan dersler ve tavsiyeler. Memory'den MEZUN EDİLEN öğrenimlerin çapalı tek satırlık kaydı da buraya düşer ("<öğrenim> artık <test/lint/CI/validator/guard> tarafından yakalanıyor — memory'den mezun edildi") — kanon: .claude/commands/devflow/lib/memory-sistemi.md → Supaplar. Kayıt faz ✅ damgalanmadan ÖNCE yazılır. -->

- **Sıradaki faz "Görsel ve mobil iyileştirme" ve kapsam sorusu hazır bekliyor:** B-032 · B-033 · B-031 atandı, ama B-031'in atomu düzeltmenin **B-030 ile aynı turda** yapılmasını istiyor (yöntem düzeltilip çıkış kodu eklenmezse ihlaller görünür olur ama kapı yine yeşil kalır) ve B-030'un bugünkü evi "Kalite kapıları otomatik". İkisinin birlikte mi yürüyeceği o fazın kapsam tartışmasının ilk kararı olsun.
- **Yeni bir dış gönderim/çağrı yüzeyi açan her task, "alıcıyı/hedefi kim doğruluyor" sorusunu kendi test kriterine yazsın.** Bu fazda soru UAT'a kaldı ve bir düzeltme task'ı doğurdu. Ölçüt biçim değil sahipliktir: `isValidEmail` adresin *biçimini* doğruluyordu, *kime ait olduğunu* değil.
- **B-019 artık iki fazın ölçümünü bozdu — çaresi F6.2'nin kapsamına adıyla girsin:** `perf.mjs` ve `font-guard.mjs`'in hedefi env ile yönlendirilebilir olsun ya da tek komut üretim konteynerini kendisi tazelesin. Bugünkü hâlde her faz kapanışı aynı el işini tekrarlıyor.
- **Alan adı geçişi fazı dört kalem devraldı ve dördü de o fazın UAT senaryosu olmalı:** Faz 1'in üç env değeri (`LEAD_STORE_TOKEN` · `IP_HASH_SALT` · `NEXT_PUBLIC_UMAMI_WEBSITE_ID`) + **B-011** (beş MX kaydı ve Google'da kutunun varlığı; yasal metnin otuz gün taahhüdü tam o gün gerçek olur). B-059'un kalan parite ayağı da orada.
- **`docs/DECISIONS.md`'nin arşiv supabı artık iki kez işledi** (09-10..09-13 ve bu turda 09-14..09-22). Doğal kesim eşiği faz kapanışıdır — sonraki faz kapanışı da ölçsün, çünkü doküman append-only olduğu için temizlikle küçülemiyor, yalnız mezuniyetle.
- **Memory bu turda ne büyüdü ne mezun verdi** (bilinçli): fazın bütün tekrar eden dersleri hâlihazırdaki dört atoma yazılmıştı (`urun-iddiasi-capa-dogrulamasi` → 7. kural, `hiz-sinirli-uca-test-bataryasi` → ikinci sayaç). Mezuniyet adayı yok: yeni kapılar (`capabilities` · `legal-consistency`) **belirli** sabitleri çiviliyor, atomların taşıdığı **genel** disiplini değil.

### Task-Spesifik Teknik Öğrenimler

<!-- OPSİYONEL: Bu fazdaki task'larda öğrenilen ama proje genelinde geçerli olmayan teknik nüanslar (araç davranışı, framework bug'ı, vb.). MEMORY.md'nin değil, faz retrosunun evidir. Bu fazda böyle bir nüans çıkmadıysa bu alt bölümü tamamen sil. -->

- **Türkçe'de regex'in `/i` bayrağı fail-open'dır.** `"EN HIZLI BÜYÜYEN ŞUBE"` dizgesinde `/en hızlı/i` **kaçırır** (I, ı ile kıvrılmaz) ve `.toLowerCase()` de kaçırır; yalnız `.toLocaleLowerCase("tr")` eşleşir. `claim-leak.mjs` bu yüzden önce `trLower()` ile normalize eder ve tüm kalıpları küçük harfle tutar.
- **`REPLACEMENTS` düz metin değişimidir ve sıraya duyarlıdır.** "Gizem" → "Yasemin" kuralı "Gizem Örge" kuralından önce koşarsa sonuç "Yasemin Örge" olur; eşlemeler **en uzun önce** sıralanır. Düşürme (`DROP_NODES`) değişimden önce koştuğu için düşen düğümlerdeki adlar hiç görülmez.
- **Ad tablosunu iddia tablosuyla birleştirmek yasaklı ad kümesini çöpe çevirir.** Ölçüldü: iddia cümlesi `REPLACEMENTS`'a konduğunda "ciro" ve "doluluk" birer *yasaklı ad* oldu ve yedi ekran kırmızıya düştü — bu yüzden `CLAIM_REPLACEMENTS` ayrı tablodur, ikisi yalnız `TEXT_FIXES`'te birleşir.
- **Kararlaştırılan bir DNS kaydı kümesi "tahmin" değil "kopya" olarak alınabilir.** B-011'in hedef MX kümesi aynı Google hesabındaki çalışan bir alan adından (`kiwiailab.com`) ölçülerek alındı; belge okumaya göre tek farkı, yanlış olamayacak olması.

### DevFlow'a Öneri

<!-- OPSİYONEL: Bu fazda fark edilen, DevFlow yönteminin geneline dair (proje-özel OLMAYAN) iyileştirmeler — aracın kendisinin nasıl çalışması gerektiği. Buraya yazılır + kullanıcıya bildirilir; DevFlow'a ayrı oturumda taşınır. Disiplin çıkmadıysa bu alt bölümü tamamen sil. -->

- **`discuss-phase` Adım 7, DURUM'un `Adım` alanına her hâlde `research` yazdırıyor — oysa aynı komutun Adım 6'sı aktif bir fazın *yeniden* tartışılabileceğini kendisi tanıyor.** Bu fazın 2026-09-23 yeniden tartışmasında harfiyen uygulansaydı, 19 task'ı bitmiş ve kabul testine hazır bir faz araştırma adımına geri gönderilecekti; oturum `verify` yazdı ve gerekçesini rapora koydu. Öneri: alan yeniden-tartışma kipinde **korunsun** ya da komut kipini ayırt etsin. (Kaynak: `BULGULAR.md` → Gelen Kutusu, `[PHASE-2]`.)

---

## Kalite Kontrol Sonuçları

> `QUALITY.md`'nin **on ekseni** (yedi standart + üç projeye özgü: 8 Dönüşüm, 9 Ölçülebilirlik, 10 İddia Uyumu). Güvenlik ekseni faz-penceresi diff'i üzerinde değerlendirildi (`e31331f..HEAD` — 30 commit / 99 dosya, +9.800/−393 satır; 1c koşumundan sonra pencereye giren tek commit UAT kaydıdır ve yalnız doküman taşır, ürün kodu değişmedi). **UAT tazeliği:** son `docs(phase-2): UAT` commit'i HEAD'in kendisidir, yani tablodaki ✅'ler bugünkü kodun üstünde ölçüldü.

| # | Eksen | Durum | Not |
|---|-------|-------|-----|
| 1 | Modülerlik | ✅ | Faz tek kaynak sayısını **artırdı**: yetenek/yol haritası `content/product.ts` → `CAPABILITIES` (altı tüketicinin altısı bağlı, elle yazan ev 0 — UAT #1) ve onay e-postası metni `content/mail.ts` (bileşende/uçta değil). Yasaklı iddia sözlüğü `research/lib/claim-leak.mjs`'te tek evde ve M6 F6.4 aynı dosyayı devralacak — ikinci liste açılmayacak. ⚠ Kalan: `SectionHead`/`PageHero`'nun 103 değerinden 3'ü içerikten (B-039, "Metin tonu" fazının ön koşulu) |
| 2 | Güvenlik | ⚠️ | **Fazın kendi yüzeyi kapandı:** UAT 1. tur `toLeadEmail`'in doğrulanmamış bir adrese gönderdiğini buldu; TASK-2.21 iki kapı koydu (adres başına 24 saatte 3 + parametresiz onay metni) ve `confirmCapped` kardeşi `HITS`'in ölçülmüş iki kusurunu **bilinçle almadı** (reddedilen deneme sayaca yazılmaz, harita dolunca `clear()` yerine yalnız süresi geçmiş budanır). Sırlar: üretim imajında `.env` yok, istemci paketinde 64-hex ve anahtar adı **0** (UAT #15/#17); hata gövdeleri hedef/yol/anahtar sızdırmıyor (#30). Faz penceresinde **yeni** bulgu yok. ⚠ Açık 🔴/🟡'ler kapsam dışı ve kanvasta: B-037 (content-type'sız POST · `null` gövde 500 · 5 MB gövde) · B-054 · B-020 · B-016 (CSP yok) · B-062 (→ QUICK-002) |
| 3 | Bakım Maliyeti | ✅ | Ölçülmüş her karar rakamıyla kod yorumunda: sözlüğün ayraç ölçümü (859 değerde "ciro" 22 / "doluluk" 15, çoğu meşru), `neg` kontrastları (5,25 / 4,96), compose'un bilinçli-boş env bloğu ve `@next/env`'in *"yalnız tanımsızı doldurur"* kapısı. `.dockerignore` niyeti yazılı. ⚠ Bilinen borç **kapılı**: yol haritası terimleri `research/lib/` altında elle tutuluyor (araştırma konteyneri `src/`'i görmüyor) ama `tests/iddia-metinleri.test.ts` her terimi `CAPABILITIES.simdi`'ye karşı doğruluyor — bayatlarsa kırmızı döner. ⚠ `npm run lint` hâlâ kırık ve ona ulaşan otomatik yol yok (B-028) |
| 4 | Performans | ✅ | Regresyon yok (UAT #27): masaüstü **141 KB / LCP 84 ms / CLS 0,005** · mobil **132 KB / LCP 64 ms / CLS 0** — çizgi 144/133 KB ve 96 ms. Faz ürün görseli toplamını **275.172 → 258.782 B**'ye indirdi (dört görsel yeniden üretildi). ⚠ Çizginin kendisi tartışmalı (B-035 — ağırlık muhasebesi JS/CSS'e kör) ve `perf.mjs` 3100'e çivili (B-019) |
| 5 | Hata Yönetimi | ✅ | Hedefsiz uç dürüst `503 no-sink` + WhatsApp yolu (#16), `{}` ile ayırt edildi (422 `missing`). Onay e-postasının başarısızlığı ziyaretçinin yanıtını **değiştirmiyor** (#24) ve "gönderilmedi"nin iki anlamı ayrı taşınıyor (`skipped` = denenmedi / `failed` = sağlayıcı reddetti) — boolean'a sığmadığı ölçüldü. Ziyaretçi tarafında faz iki boşluğu kapattı: alan bazlı hata metni + `aria-invalid` (16/16 doğru işaret, yanlış alarm 0) ve odağın hata türüne göre doğru hedefe gitmesi (16/16). ⚠ Çalışma zamanı alarmı hâlâ yok (B-025); üç sessiz kayıp yolu açık (B-036) |
| 6 | Test Kapsamı | ✅ | Batarya **180 → 216 geçti + 1 atlandı** (bu oturumda yeniden ölçüldü; anahtarsız 209 + 2), `tsc --noEmit` çıkış **0**. Faz üç yeni dosya getirdi — `capabilities` 62 · `iddia-metinleri` 46 · `legal-consistency` 31 — ve **hiçbiri kör kabul edilmedi**: yazım sırasında 12 + 14 negatif kontrol (ikisi kapının kendi fail-open'ını buldu), UAT'ta dokuz ters çevirme; hepsi kırmızı. ⚠ CI yok (F6.3) ve kapıların dördü eşik altında bile çıkış 0 veriyor (B-030) — "yeşil" hâlâ elle okunuyor. ⚠ Tarayıcı katmanı bilinçle otomatik değil: B-055/B-034 kriterleri `kanal: UAT` işaretli, kalıcı betik "Kalite kapıları otomatik" fazında |
| 7 | Erişilebilirlik | ✅ | `a11y.mjs` **TOPLAM SORUN 0**, `mobile-audit` 9/9 rotada yatay kaydırma yok, `font-guard` 16 sayfada eksik karakter yok, `scan` üç rotada konsol temiz (#27). Faz üç a11y borcunu kapattı: onay kutusu 320-412 px'te **4/4** tam görünür (üst kenar 88 px = `scroll-padding-top`), hatalı alan görsel işaret + alan altı metin (kırık `aria-describedby` **0**), mobil ana çağrı 24 → **52 px** (12/12 örnek). ⚠ Ölçülmüş AA ihlalleri (B-032) ve 320 px'te içerik kaybı (B-033) kapsam kararıyla sıradaki fazda; ölçüm aracının kör noktaları (B-031) ve rota kapsamı (B-012) açık |
| 8 | Dönüşüm | ✅ | ILKELER'in 1. ekseni ve fazın ikinci amacı: huninin son metresi kapandı — mobilde onay **ve** hata görünüyor, ana çağrı dokunulabilir, talep sahibi onay e-postası alıyor. Güvenlik kapısı akışa **hiç dokunmadan** kondu (uç yine 200, kayıt yine yazılıyor, ekip bildirimi yine gidiyor); çift-katılım tam bu eksene çarptığı için bilinçle reddedildi (`docs/DECISIONS.md` 2026-09-23). ⚠ Açık: mobilde ilk ekranda dönüşüm yüzeyi yok (B-022), hız sınırı geçerli talebi reddediyor (B-020) |
| 9 | Ölçülebilirlik | ✅ | Faz yeni dönüşüm yüzeyi açmadı, mevcut zincir canlı önizleme yüzeyinde yeniden doğrulandı: `data-exclude-search` çalışıyor (`/demo?q=…` gezildi, olay yükündeki `url` sorgu dizesini taşımıyor), yükte ad/telefon/e-posta yok, dış istek yalnız kendi Umami'miz (#29). `notify_lead` artık gerçek sonucu taşıdığı için **onay kanalı panelden okunabilir** hâle geldi — kalıcı `pending` kalkmadı, ölçülebilirlik kazanıldı |
| 10 | İddia Uyumu | ✅ | Fazın çekirdek amacı. Sekiz yasaklı ifadenin "bugün var" bağlamında **0'ı** (#2; kalan 6 vuruşun altısı da "Yolda"/"Yol haritasında" listelerinin içinde), rakip adı **0 gerçek ihlal** (#3), yasal metinde uydurma süre/sağlayıcı/madde numarası **0** (#11), ürün görselinde ad ve iddia sızıntısı **0** (#6). Ve iddia artık **kapılı**: fail-closed yayın kapısı · yasal beyan kapısı (9 dal) · görsel denetimin dört dalı (sözlük 20 kalıp, dizge ölçümü 3/20 → 10/20). ⚠ Metin tarafının otomatik denetimi hâlâ yok — evi M6 F6.4 ve **aynı sözlüğü devralacak** |

**Kullanıcı yolculuğu ve boşluk tespiti (Adım 3b):** Faz 1'in kapanışında işaretlenen tek gerçek dikiş bu fazda kapandı — ziyaretçi artık talebinin ulaştığını **iki kanaldan** öğreniyor (mobilde görünen ekran onayı + onay e-postası), hata hâlinde ise hem alanında işaret hem WhatsApp yolu görüyor. Uçtan uca akışta sahipsiz yeni bir yer bulunmadı; bugün açık kalan kopukluklar (mobilde ilk ekranda çağrı yokluğu, 320 px'te içerik kaybı, AA kontrast) **atanmış** kalemlerdir ve hepsi sıradaki iki fazın kapsamındadır. Kullanıcı gözüne kalan üç kalem (gerçek telefonda form denemesi · onay e-postasının gelen kutusu/spam yerleşimi · ekran okuyucuda onay kutusunun iki kez duyurulması) milestone kriterlerini değil **doğrulama kanalını** ilgilendiriyor; üçüncüsünün kaydı `BULGULAR.md` → Gelen Kutusu'nda.

---
