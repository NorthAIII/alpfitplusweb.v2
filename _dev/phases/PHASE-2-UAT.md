# Phase 2 — UAT Detayı

← PHASE-2 · uat

> `_dev/phases/PHASE-2.md` → UAT Sonuçları'nın bölme çocuğudur (verify-phase boyut kapısı, 2026-09-23; faz **hâlâ aktifken** bölündü — UAT tablosu ve otomatik kontrol dökümü yazıldığında parent 23.517 token'a çıkarak kırmızı çizgiyi aştı). Parent'ta sonucun **snapshot'ı** durur (tarih, sayı, ölçüm yüzeyi, kalan kalem — CLAUDE.md → Faz dokümanı hibrittir: "UAT sonuç tablosu — özet kalır"); **senaryoların tam tablosu ve otomatik kontrol dökümü buradadır.**

---

## UAT Sonuçları — tam tablo

**Tarih:** 2026-09-23 (2. tur — TASK-2.21 sonrası yeniden koşum; bütün kontroller baştan yapıldı)
**Toplam Senaryo:** 31 | **Geçen:** 31 | **Kalan:** 0

> **Tur farkı:** 1. turun 30 senaryosu baştan ölçüldü (sonuçlar sıfırlandı, kanıt notları bu turun ölçümleriyle yeniden yazıldı — önceki turun notları taşınmadı). **31 yeni senaryodur:** TASK-2.21 bir tavan sayacı getirdi ve task dokümanı hız sınırıyla etkileşiminin kontrol edilmesini istemişti; o kontrol 1. turda yoktu.

| # | Senaryo | Sonuç | Not |
|---|---------|-------|-----|
| 1 | Altı yüzey (Kurucu Programı · /ozellikler · asistan · SSS · /fiyat · karşılaştırma) aynı yol haritası listesini gösteriyor; kalem sayısı ve sırası tek kaynakla birebir | ✅ Geçti | `CAPABILITIES` bugün 12 «bugün var» + 7 «yolda» + 5 «yol haritasında»; altı tüketicinin altısı da sabitten okuyor (`capabilityProse` · `stageNote` · `capabilityTitle`/`upcomingCapability`), elle yazan ev yok · kapı `capabilities.test.ts` 62 test yeşil · **ters-çevirme:** satır 5'te |
| 2 | B-029'un beş karşılıksız iddiası hiçbir yüzeyde "bugün var" olarak görünmüyor | ✅ Geçti | sekiz yasaklı ifadenin **«bugün var» bağlamında 0'ı**; yayındaki HTML'de kalan 6 vuruşun **altısı da «Yolda» / «Yol haritasında» listelerinin içinde** (konum konum okundu: anasayfa 4 · ozellikler 2) — fazın tasarladığı hâlin ta kendisi · **kontrol:** düzeltilmiş ifadeler bulunabiliyor (*bitişe yaklaşan üyeler listesi* 2 · *toplu duyuru* 4 · *pilot* 3), sonda kör değil |
| 3 | Pilot cümlesi yalnız `PRODUCT_STATUS`'tan, fiyat yalnız `monthlyFor()`'dan geliyor; sitede rakip adı geçmiyor (CLAIMS sınırı) | ✅ Geçti | pilot cümlesi tek evde (`site.ts:74`), dört tüketici sabiti okuyor · `src/`'de elle yazılmış dört haneli ₺ rakamı **0** · 18 marka adı 10 sayfada tarandı: tek vuruş **«CrossFit» = segment adı** (108 vuruşun tamamı), gerçek ihlal 0 |
| 4 | **Adversarial:** bir yol haritası kalemi "bugün var"a taşındığında derleme ve servis duruyor (fail-closed yayın kapısı) | ✅ Geçti | **ters-çevirme:** `qr-turnike` → «bugün var» → batarya **216 → 154** (`capabilities.test.ts` modül düzeyinde düştü, 62 test kayboldu) · `/ozellikler` ve `/fiyat` **200 → 500** · geri alınca ikisi de 200 |
| 5 | **Adversarial:** bir tüketiciye sabiti atlayan kalem elle yazıldığında test bataryası kırmızı dönüyor | ✅ Geçti | **ters-çevirme:** `/ozellikler`'e elle `<li>QR ve turnike ile giriş</li>` → **tam 1 kırmızı, doğru testte** (*B-040 — yol haritası kalemi bileşende elle yazılmaz › 'src/app/ozellikler/page.tsx' sabiti atlayan kalem taşımıyor*) |
| 6 | Yayındaki yedi ürün görselinde gerçek kişi adı, "Kampanyalar" menü girdisi ve karşılanmayan özellik kartı yok | ✅ Geçti | hat sıfırdan koşturuldu (kaynak `../Alpfit.v1/demo` salt okunur): *"Denetim: her ekran ad, marka, İDDİA ve GÖRSEL sızıntısı için tarandı, sızıntı yok"*, çıkış 0 · taze çıktı ↔ `public/product/` **7/7 md5 birebir** — yayındakiler denetimden geçmiş çıktının kendisi · `finans.webp` 1440×**601** (bir kez görülen 606 sapması yinelenmedi) |
| 7 | **Adversarial:** kaynağa ad sızıntısı enjekte edildiğinde hat sıfır-olmayan kodla duruyor ve dosya yazmıyor | ✅ Geçti | **ters-çevirme:** grup.html'in 4 ad eşlemesi söküldü → `[grup] DENETİM BAŞARISIZ — ad sızıntısı: ["Ferdi Kadıoğlu","Şehmus Hazer"]`, hat durdu, **`grup.webp` yazılmadı** (7 yerine 5 dosya) · geri alındı, md5 tur başıyla birebir |
| 8 | **Adversarial:** kaynağa iddia dizgesi (ciro projeksiyonu / üstünlük rozeti) enjekte edildiğinde hat duruyor | ✅ Geçti | **ters-çevirme:** cockpit'in 9 düşürme kuralı söküldü → **20 iddia bulgusu, beş sınıfın dördü birden** (büyüme kıyası · üstünlük · ROI/projeksiyon · müşteri sayısı), **0 dosya yazıldı** — B-018'in arızası birebir yeniden üretildi |
| 9 | Denetimin kapsamı çökerse (sözlük · ad tablosu · toplanan metin kütlesi) hat yeşil koşmuyor, alt sınırda duruyor | ✅ Geçti | **ters-çevirme:** sözlük 4/16'ya budandı → `[claim-leak] sözlük çöktü — 4/16 kalıp. Denetim kapsamsız koşmaz.` **import anında** durdu, 0 dosya |
| 10 | Yayındaki yasal metin IP'nin iki kullanımını, 12 ay saklamayı, dört tedarikçinin ülkesini ve ölçüm sunucusunun erişim kaydını anlatıyor | ✅ Geçti | üç yasal sayfanın render edilmiş metninde: *IP adresiniz* 10 · *on dakika* 1 · *12 ay* 3 · *Washington* 1 · *Nürnberg* 2 · *İrlanda* 1 · *erişim kayd…* 5 · *silme süresi işletmiyoruz* 2 · sağlayıcı adları Vercel/Resend/Hetzner/Google 1'er |
| 11 | Yasal metinde uydurulmuş süre, sağlayıcı ya da hukuki dayanak yok; her olgu cümlesinin koddaki karşılığı duruyor | ✅ Geçti | metindeki **tüm** süre ifadeleri sayıldı: rakamla yalnız «12 ay» (ölçülü), yazıyla «on dakika» (ölçülü) · «otuz gün» ve «on gün» (KVKK'nın kendi süreleri) · «bir ay» · uydurma madde numarası (`m.NN` / `madde NN`) **0** · adı geçen 5 sağlayıcının hepsi TASK-2.17'de kaynağından ölçülmüş |
| 12 | Dokuz dallık yasal beyan kapısı yeşil koşuyor (çapraz depo anahtarı tanımlıyken) | ✅ Geçti | `docker compose exec -e LEGAL_CONTRACT_HOOKS_DIR=/opt/v1-pb-hooks web npm test` → **216 geçti + 1 atlandı** (9 dosya); `legal-consistency.test.ts` 31 test |
| 13 | **Adversarial:** bir beyanın dayanağı bozulduğunda kapı kırmızı dönüyor — sessizce geçmiyor | ✅ Geçti | **ters-çevirme ×2.** Metin tarafı (`12 ay` → `24 ay`, 4 yerde): **2 kırmızı** (*dal 9 › ölçülen ay sayısıyla tam bir kez geçiyor* + *Saklama süresi bölümündeki HER ay sayısı*). Kod tarafı (`data-exclude-search` sökülünce): **2 kırmızı** (*boş kapsam bekçisi* + *dal 1 › dayanak*) |
| 14 | Çapraz depo anahtarı tanımsızken dal atlanıyor ve bataryanın **geçen** sayısı birebir korunuyor | ✅ Geçti | anahtarsız **209 geçti + 2 atlandı** · anahtarlı **216 + 1** → fark tam **7** = dal 9'un test sayısı, TASK-2.19'un ölçtüğü farkla birebir (211−204). Keyless sayı yalnız TASK-2.21'in eklediği 5 testle arttı (204 → 209) |
| 15 | Üretim imajında `/app/.env` yok; imaj genelinde hiçbir `.env` izi yok | ✅ Geçti | **taze imaja karşı** (`alpfitplus-web-web-prod:latest`, 17:40): `/app/.env` yok, imaj genelinde `.env*` globu **boş** · **kontrol:** aynı `find` kalıbı `pack*` ile vuruş veriyor — sonda gerçekten dosya sistemini geziyor |
| 16 | Yerel üretim provası hedefsiz: geçerli talep `503 no-sink` alıyor, hiçbir depoya satır yazılmıyor | ✅ Geçti | taze imajdan koşan geçici konteyner (3200, `web-prod` ile birebir env): geçerli talep → **`503 no-sink`**, log *"Depo yapılandırması eksik, kayıt denenmedi"* · **kontrol:** `{}` → **422 `missing`** (istek yolu sağlam, 503 sessiz bir çökme değil) |
| 17 | Sırlar istemci paketine sızmıyor — üretim çıktısında token, tuz ya da API anahtarı yok | ✅ Geçti | `.next/static`'te 64-hex dizge **0**, anahtar adı (`LEAD_STORE_TOKEN`/`IP_HASH_SALT`/`RESEND_API_KEY`/`LEAD_TOKEN_*`) **0** · **kontrol:** «alpfitplus» aynı ağaçta **3 dosyada** bulunuyor |
| 18 | 320/360/390/412 px'te gönderim sonrası onay kutusu tam görünür ve yapışkan başlığın arkasında değil | ✅ Geçti | kanal: UAT · gerçek tarayıcı, üretim yüzeyi · **4/4 genişlikte** kutu tam görünür, üst kenarı tam **88 px** (= `scroll-padding-top`; başlık 68 px), odak **4/4** `role=status` üzerinde |
| 19 | Aynı dört genişlikte hatalı alan görsel işaret alıyor ve hata metni alanın hemen altında görünüyor | ✅ Geçti | kanal: UAT · **16/16** hâlde işaretleme doğru ve **yanlış alarm yok**: `missing` (ad boş, kulüp dolu) → yalnız `name` işaretli, kulüp değil (ayıklama çalışıyor) · `missing-contact` ve `bad-contact` (ikisi de hatalı) → `phone`+`email` · eşlenmeyen `no-sink` → 0 alan · **kırık `aria-describedby` 16/16 hâlde 0** |
| 20 | Odak hata türüne göre doğru yere gidiyor: boş alan · dolu-ama-bozuk alan · hata kutusu | ✅ Geçti | kanal: UAT · **16/16** odak doğru hedefte (4 genişlik × 4 hâl): `missing`→`name` · `missing-contact`→`phone` · `bad-contact`→`phone` · `no-sink`→`#demo-form-error` · ⚠️ ilk sonda **uydurma** bir hata koduyla kurulmuştu ve 8/12 okumuştu; kusur üründe değil sondadaydı, gerçek kod kümesiyle yeniden kuruldu |
| 21 | Genel hata kutusu ve WhatsApp yedeği duruyor — hata anında bile talep yolu açık | ✅ Geçti | **16/16** hata hâlinde özet kutusu görünür **ve** WhatsApp bağlantısı sayfada duruyor |
| 22 | Fiyat hesaplayıcısının mobil ana çağrısı altı rotada ≥ 52 px | ✅ Geçti | kanal: UAT · 390 px · altı rotanın altısı da HTTP 200 ve 2'şer CTA taşıyor → **12/12 örnek tam 52 px** (`/` · `/fiyat` · dört segment sayfası) |
| 23 | Talep sahibine onay e-postası gidiyor; `notify_lead` üç değerin doğrusunu taşıyor (`sent` / `skipped` / `failed`), hiçbir kayıt `pending` kalmıyor | ✅ Geçti | **ters-çevirme:** `notify_lead` «sent»e sabitlendi → **11 kırmızı**, her biri kendi dalını adıyla söylüyor (kanal kapalı→failed · adressiz→skipped · bozuk adres→skipped · yalnız onay reddedildi→failed · tavana takıldı→skipped) |
| 24 | Onay gönderimi ziyaretçinin yanıtını değiştirmiyor: üç dalda da aynı `200` ve aynı gövde alanları | ✅ Geçti | aynı ters-çevirmede *"uç yine 200 stored:true"* testi kırmızı verdi → dal gerçekten ölçülüyor, yeşil bedavaya gelmiyor |
| 25 | Bal küpü dolu istek: kayıt yok, e-posta yok, HTTP `200` (bot yanıltması korunuyor) | ✅ Geçti | canlı uçta bal küpü dolu istek → **200 `{"ok":true}`** ve konteyner logunda o istek için **hiçbir depo satırı yok** (aynı turdaki üç geçerli talep log'a düştü, bu düşmedi) · **ters-çevirme:** kapı sökülünce *"bal kupu dolu -> 200; depoya da, e-postaya da hicbir cagri yok"* testi **1 kırmızı** |
| 26 | **Adversarial / güvenlik:** uç, talep sahibinin yazdığı adrese doğrulama yapmadan e-posta gönderiyor mu — ve bunu hangi kapılar sınırlıyor? | ✅ Geçti | **TASK-2.21 sonrası bugünkü hâl ölçüldü.** Taze üretim imajında onay metni **parametresiz** (`Merhaba,`) — bayat konteyner hâlâ `Merhaba ${r},` taşıyordu, ayrım bu turda ayırt edici alan olarak kullanıldı. Tavan: adres başına 24 saatte 3. Batarya sondası *"üçüncü bir adrese art arda tetikleme ilk 3'te tavana takılıyor"* yeşil · **ters-çevirme:** tavan sökülünce **2 kırmızı** (kötüye kullanım sondası + büyük/küçük harfle atlatma). **Kapanmayan artık, bilinçle:** adresin sahipliği *doğrulanmıyor*; çift-katılım dönüşüm yoluna dokunduğu için alınmadı (`docs/DECISIONS.md` 2026-09-23), kalan yüzey tavanlı ve içeriksiz |
| 27 | Beş ölçüm başlangıç çizgisinin altında: a11y TOPLAM SORUN 0 · yatay kaydırma yok · eksik karakter yok · konsol temiz · ağırlık ve LCP | ✅ Geçti | a11y **TOPLAM SORUN 0** · mobile-audit **9/9 rotada yatay kaydırma yok** (taşan eleman 3 = bal küpünün bilinçli konumu; dokunma hedefi 157, taban birebir) · font-guard 153 karakterlik küme / 16 sayfa / **85.015 karakter**, eksik yok · scan `/demo` `/kvkk` `/fiyat` **üçünde de konsol temiz** · perf masaüstü **141 KB / LCP 84 ms / CLS 0,005**, mobil **132 KB / LCP 64 ms / CLS 0** → çizgi 144/133 KB, 96 ms: **regresyon yok** |
| 28 | `npm test` yeşil, `tsc --noEmit` çıkış 0, üretim derlemesi hatasız | ✅ Geçti | batarya **216 + 1** · `tsc --noEmit` çıkış **0** · üretim imajı bu turdan önce HEAD'ten derlendi ve çıkış 0 aldı (imajın kendisi ölçüldü: taze) |
| 29 | Ölçüm yükünde kişisel veri yok; arama sorgusu ölçüme gitmiyor (`data-exclude-search`) | ✅ Geçti | **gerçek tarayıcıda, canlı önizleme yüzeyinde** (`alpfitplus-web-v2.vercel.app`): `script.js` yüklendi, `data-website-id` VAR, `data-tag=preview`, `data-exclude-search="true"` · dış istek **yalnız kendi Umami'miz** (2 adet) · **belirleyici ölçüm:** `/demo?q=gizli-arama-terimi&utm_source=uat` ile gezildi, olay yükündeki `url` **`…/demo`** — sorgu dizesi yükte **yok**; yükte ad, telefon, e-posta da yok |
| 30 | Hata mesajları hedef adresini, dosya yolunu ya da anahtarı sızdırmıyor | ✅ Geçti | beş ayrı hata gövdesi okundu (503 `no-sink` · 422 `missing` · 422 `no-consent` · 400 `bad-json` · bal küpü 200) — hiçbirinde hedef adresi, dosya yolu ya da anahtar adı yok |
| 31 | **Yeni (TASK-2.21 devri):** adres başına tavan, hız sınırıyla ve kapalı kanalla çakışmıyor — gönderilmeyen e-posta adresin kotasını yakmıyor | ✅ Geçti | kaynak sırası okundu: env kapısı (`!key \|\| !team \|\| !from` → `failed`) tavandan **önce** koşuyor (`route.ts:370` ↔ `:373`) · **ters-çevirme:** tavan env kapısının önüne alındı → **1 kırmızı**, adıyla o invaryantı söyleyen test (*"kanal kapalıyken sayaç YANMIYOR: gönderilmeyen e-postalar adresin tavanını tüketmiyor"*) · hız sınırı ayrı eksende ve doğrulamadan önce koşmaya devam ediyor (B-020, açık bulgu — bu faz onu kapsamıyor) |

---

### Otomatik Kontroller (Adım 1)

- **CI/CD yok** (M6 F6.3 gelecek) — `.github/` dizini bulunmuyor. Push sonrası tek otomatik sinyal **Vercel derlemesidir**: listelenen **13 dağıtımın 13'ü de `Ready`**, başarısız dağıtım yok; en yenisi TASK-2.21'in push'u.
- **Bağımlılık/güvenlik botu kurulu değil.** `npm audit` → **0 açık**. `npm outdated` dokuz paketi geride gösteriyor (Next 16.3.4→16.3.6, React 19.2.8→19.3.0, Vitest 4→5, TS 5.9→7) — kayıt zaten `BULGULAR.md` → Gelen Kutusu'nda (`[PHASE-1]`), ikinci satır açılmadı.
- **Güvenlik taraması (faz penceresi `e31331f..HEAD`, 29 commit / 98 dosya).** 1. tura göre iki commit eklendi (UAT kaydı + TASK-2.21); yeni kod merceğe alındı: enjeksiyon (SQL/komut/yol), yetki atlaması, gömülü sır ve hassas veri loglaması **bulunmadı**. `confirmCapped` ziyaretçi girdisini yalnız harita **anahtarı** olarak kullanıyor, onay metni parametre almıyor, `pruneConfirmHits` `clear()` yerine süresi geçmişi buduyor (sınırsız büyüme yok). **Bu turda yeni bulgu çıkmadı** — 1. turun tek bulgusu (senaryo 26) TASK-2.21 ile kapandı.
- **Artefakt süpürmesi.** Fazın tanıttığı **yeni** ortak kapı `confirmCapped`'tir; korunan işlem "ziyaretçinin verdiği adrese gönderim". Çağrı sitesi sayıldı: `api.resend.com`'a giden **iki** gönderim var — `route.ts:315` ekip bildirimi (`to: [to]`, alıcı sabit `DEMO_TO`, invaryanta tabi değil) ve `route.ts:380` onay (`to: [lead.email]`, `:373`'te tavana tabi). **Atlayan site yok.** Önceki üç kapı da yeniden sayıldı: yetenek tek kaynağı (altı tüketicinin altısı bağlı), görsel denetim (hattın tek girişi `render-product.mjs`, yedi ekranın yedisi denetimden geçiyor), `CONTACT.support` (`src/`'de elle yazılmış adres **0**).
- **Kayıt süpürmesi.** Gelen Kutusu'nun bu faza dokunan satırları ve işaretsiz açık bulgular incelendi; **çözülmüş çıkan olmadı.** TASK-2.21 `route.ts`'e dokunduğu için kardeş bulgular ayrıca ölçüldü ve **dördü de açık**: B-037 (content-type'sız POST kabul ediliyor · `null` gövde **500** · 5 MB gövde kabul), B-054 (`0000000000` doğrulamayı geçiyor), B-020 (`limited(ip)` `:399`, doğrulama `:434`'ten önce). **B-019 bu turda taze kanıtla doğrulandı** — aşağıda.

### Ölçüm yüzeyi ve bir ölçüm tuzağı

**Yüzeyler:** `web` konteynerinde Vitest (9 dosya), araştırma konteynerinde Playwright (320/360/390/412 px ve 390 px), **taze üretim imajından koşan geçici konteyner (3200)**, dev sunucusu (3000), canlı önizleme (`alpfitplus-web-v2.vercel.app`), görsel üretim hattı (kaynak salt okunur), beş kapı betiği, Vercel dağıtım listesi.

⚠️ **Çalışan `web-prod` konteyneri (3100) BAYATTI ve bu tur onu kullanmadı.** Ölçüldü: konteyner `sha256:f757…` (07:41Z) imajından koşuyor, en yeni imaj ise 17:40'ta derlenmiş `90f71e89`; bayat konteynerde `confirmCapped` **0 eşleşme** ve onay metni hâlâ `Merhaba ${r},`. Yani 3100 **TASK-2.21 öncesi kodu sunuyor** — imaj taze, konteyner değil (`build` konteyneri yeniden yaratmaz). Bu, açık bulgu **B-019'un mekanizmasının** taze kanıtıdır; bulgu zaten kanvasta olduğu için ikinci satır açılmadı.

Senaryo 15 · 16 · 17 bu yüzden taze imajdan koşan kendi geçici konteynerimize karşı ölçüldü. **`perf.mjs` 3100'e çivili ve env ile yönlendirilemiyor**, o yüzden önce bayat ve taze yüzeyin aynı şeyi sunup sunmadığı ölçüldü: beş rotanın render edilmiş HTML'i, Next'in `buildId`'si normalize edilince **birebir aynı** (bayt boyutları da birebir) — tek fark build kimliği. Sonda duyarlılığı ayrıca sınandı (tek jetonluk bozma farkı yakalıyor), çünkü ilk denemede `sed` sessizce boş çıktı verip **sahte bir yeşil** üretmişti. Dolayısıyla perf ölçümü bugünkü yüzeyi ölçüyor.

**Ters çevirmeler bu turda dokuz tanedir ve dokuzu da kırmızı verdi** (satır 4 · 5 · 7 · 8 · 9 · 13a · 13b · 23 · 25 · 26 · 31 — bazıları aynı bozmayı paylaşır). Hepsinde **girdi** bozuldu, kaynak değil; her bozmadan sonra dosya scratchpad kopyasından `cp` ile geri alındı. Tur sonunda `src/` · `research/` · `tests/` ağacının **99 dosyasının 99'u** tur başı md5'iyle birebir ve `git status` temiz (`git checkout` / `git restore` kullanılmadı).

**Otonom kolda kapanmayan senaryo yok** — 31/31 ölçülerek kapandı. 1. turun kullanıcıya bıraktığı üç kalem (gerçek telefonda form denemesi · onay e-postasının gelen kutusu/spam yerleşimi · ekran okuyucuda onay kutusunun iki kez duyurulması) **bu fazın milestone kriterlerini değil doğrulama kanalını** ilgilendiriyor ve yerinde duruyor; üçüncüsünün kaydı `BULGULAR.md` → Gelen Kutusu (`[TASK-2.05]`).

---
