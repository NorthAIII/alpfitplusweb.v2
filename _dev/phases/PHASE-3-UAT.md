# Phase 3 — UAT Detayı

← PHASE-3 · uat

> `_dev/phases/PHASE-3.md` → UAT Sonuçları'nın bölme çocuğudur (verify-phase boyut kapısı, 2026-09-26; faz **hâlâ aktifken** bölündü — 38 senaryonun tablosu ve otomatik kontrol dökümü yazıldığında parent 26.084 token'a çıkarak kırmızı çizgiyi aştı). Teşhis **gerçek büyüme**; kesim dokümanın kendi `## UAT Sonuçları` sınırından geçti ve bu projede emsali var (`PHASE-2-UAT.md`). Parent'ta sonucun **snapshot'ı** durur (tarih, sayı, ölçüm yüzeyi, kalan kalemler — CLAUDE.md → Faz dokümanı hibrittir: "UAT sonuç tablosu — özet kalır"); **senaryoların tam tablosu, otomatik kontrol dökümü ve kapıların kör noktaları burada.**

---

## UAT Sonuçları

> `/devflow:verify-phase` oturumunda dolduruldu (2026-09-26). **İKİNCİ TUR** — TASK-3.26 ve TASK-3.27 kapandıktan sonra bütün kontroller **baştan** koşuldu (Adım 10 kuralı): sonuçlar ⬜'ye döndü, önceki turun sorun açıklamaları ve kanıt notları silindi, `→ TASK-X.YY` işaretleri korundu. **Kol: otonom** (orkestratörlü koşum — alt ajan kullanıcıyla konuşamaz).

**Tarih:** 2026-09-26 (2. tur)
**Toplam Senaryo:** 40 | **Geçen:** 32 | **Kalan:** 8

**Küme iki senaryo BÜYÜDÜ (38 → 40)** — ikisi de onaylı düzeltme task'larının hiç UAT'lanmamış çıktısını ölçer (Adım 2c: *"kayıt research'ten sonra doğduysa hiç ölçülmemiştir"*): **39** TASK-3.26'nın beş kontrast kalemini üç genişlikte sınar, **40** TASK-3.27'nin ikinci yüzeyini (JS'te açıkça verilen kaydırma davranışı) sınar. Mevcut satırlar yeniden numaralandırılmadı. **Senaryo 3'ün metni kayıtlı kullanıcı kararına göre YENİDEN YAZILDI** (2026-09-26, seçenek B — Adım 2c: *"kayıtlı bir karar bir kriteri geçersizleştirdiyse senaryo karara göre yazılır"*): ölçüt artık `TOPLAM SORUN 0` değil, *sayfa kaynaklı ihlalin kalmaması ve kalem sayısının artmaması*.

**Ölçüm zemini — hangi senaryo hangi hedefe ölçüldü:** `a11y` · `mobile-audit` · `font-guard` · `perf` ve bu turun UAT sondaları **yayın kopyası (3100)**; `scan.mjs` **geliştirme sunucusu (3000)** — betikte sabit, `BASE` taşımıyor (senaryo 21'in kapsam notu). Batarya/tip/lint `web` konteynerinde. **Bütün ölçümler TASK-3.27'nin imajına karşı koşuldu** (`lastmod` 13:17:16.895Z) ve tazelik dört pozitif çapayla doğrulandı (`/` → `-sm.webp` **68** · `/gecis` → gizlenmiş dev rakam **1** · CSS'te opak rozet zemini **1** · hareket azaltma kaydırma sıfırlaması **1**) artı dört negatif kontrolle (olmayan varlık · eski rozet sınıfı · kaçak yardımcı sınıf · ölü hedef) **0**.

⚠️ **Ölçümlerden SONRA imaj bilerek bir kez yeniden derlendi — ve bu bir tazeleme değil, bu turun KENDİ YAZISINI sınayan bir ölçümdü.** TASK-3.27 kazara şunu ölçmüştü: `_dev/` dokümanları Tailwind taramasına dahil, yani bir doküman cümlesine yazılan sınıf adı üretim CSS'ine **gerçek bir kural ekliyor** (o turda 85.312 → 85.350 bayt). Bu tur çok sayıda `_dev/` metni yazdığı için tuzak kendi çıktısına karşı sınandı: yeniden derlemeden sonra servis edilen CSS **85.312 bayt · md5 `5f2d8774…` · chunk `3p3z86zp6cec8.css`** — üçü de tabanla **birebir**, kaçak yardımcı sınıf **0**. Yani bu turun yazısı üretim çıktısını **hiç değiştirmedi** ve yukarıdaki bütün ölçümler geçerli kalır (imajın `lastmod`'u 14:04'e döndü, servis edilen CSS aynı bayt).

| # | Senaryo | Sonuç | Not |
|---|---------|-------|-----|
| 1 | Kontrast kapısı 16 rotanın hepsini gezer, boyanan gerçek rengi ölçer, ölçemediğini sayar | ✅ Geçti | 16 rota (sitemap 15 + 1) · 105 ekran adımı · 1831 eleman · piksel yöntemi · ölçülemeyen yapışkan **151** · görünmez **43** · ekran dışı 0 · **kalan 0** |
| 2 | Kontrast kapısı eşik altında sıfır-olmayan çıkış kodu verir | ✅ Geçti | `kontrol:` bugün 1 gerçek kalemle **çıkış 1**; ölü hedefte (`BASE=…:3457`) tam cümle + **çıkış 1**, yığın izi yok |
| 3 | `a11y.mjs` 16 rotada **sayfa kaynaklı** kontrast ihlali bulmaz ve kalem sayısı artmaz — kapı yalnız kayıtlı tek ölçüm artefaktıyla (`1 · çıkış 1`) kırmızı kalır | ✅ Geçti | 16 rotanın 15'inde kontrast ihlali **0**. Kalan tek kalem `/gecis` paragrafı: `p02` **3,25** · min 3,21 · med **7,05** · glif **2955px** — dört rakam da TASK-3.26 ile **birebir**, yani belirlenimli ve **ölçümün kusuru** (B-063, kullanıcı kararı seçenek B). Kalem sayısı **1 = taban**, artmadı · → TASK-3.26 |
| 4 | B-032'nin adlandırılmış kontrast kalemleri AA'yı geçer | ✅ Geçti | 15 rotada kontrast ihlali **0** · gradyan metin **19 durak ölçüldü (taban 19) / 0 eşik altı** · `alt`sız img 0 · adsız link/buton 0 |
| 5 | Başlık hiyerarşisi 16 sayfada atlamasız, her sayfada tek `h1` | ✅ Geçti | **316 görünür başlık (taban 316) · 0 sayfada 0 atlama** · her rotada `h1:1` |
| 6 | Mobil kapı 320 ve 390 px'te kesilen metin bulmaz | ✅ Geçti | Kırpılmış taşma **0 gerçek · en ağır 0 px**, iki genişlikte; 2054 metin elemanı (taban 2054) · 6253 eleman |
| 7 | Kaydırılabilir şeritte kart kabın görünür genişliğine sığar | ✅ Geçti | Şerit ihlali **0**, iki genişlikte; 5 kaydırılabilir kap (taban 5) · iki boyutlu içerik muafiyeti 2 |
| 8 | Dönüşüme dokunan her hedef ≥ 44×44 px | ✅ Geçti | Kritik küme **305 ölçüldü / 0 eşik altı / 0 benzersiz (taban 305)**, 320 ve 390 px'te birebir |
| 9 | Gezinme kulvarı ölçülür ve raporlanır ama kapıyı düşürmez | ✅ Geçti | **333 ölçüldü / 261 eşik altı** → alt bilgi 256 · içerik yolu 4 · gövde metni 1; TOPLAM SORUN 0, çıkış 0 |
| 10 | Mobil kapı ölçemediği hâlde kırmızıya döner | ✅ Geçti | `kontrol:` ölü hedefte cümle + **çıkış 1** (yığın izi yok) ↔ 3100'de **çıkış 0** |
| 11 | Telefonda her sayfanın ilk ekranında demoya çıkan bir yol var | ✅ Geçti | **9 eksende 16/16** (boş 0): 320×568 · 390×844 · 412×915 · 768×1024 · %200 (640×512) · %400 (320×256) · yatay 844×390 ve 915×412 · 1440×900. `kontrol:` eksen başına **146 dönüşüm bağlantısı** tarandı ve kullanılabilir sayısı eksene göre **16–45** arasında değişiyor (sonda kör değil). ⚠️ İlk sondam %400 ekseninde **yanlış kırmızı** verdi (eksen etiketi zaten büyütülmüş pencereyi yazıyor, üstüne `zoom` çarpanı konunca etkin 80×64 oluyordu) — düzeltildi |
| 12 | Faydalar bölümü reddedilen kalıptan çıktı | ✅ Geçti | `#fayda` üç genişlikte (1440/390/320): **ızgara kabı 0 · ikon karosu 0** · gerçek fotoğraf **1**. `kontrol:` aynı sonda `#neden`de **1 kap + 4 karo**, `#segmentler`de **1 kap + 4 foto** buluyor — dedektör kör değil |
| 13 | Modüller'in tırtıklı 5'li ızgarası yeniden kuruldu | ✅ Geçti | `#moduller` @1440 **1 kap (5 sütun) + 5 karo**, @390 ve @320 **1 kap (1 sütun) + 0 karo** — kalan kap ve karolar kullanıcı kararıyla duran 5'li ikon şeridinin; öne çıkanların katkısı **0** |
| 14 | İki yeniden tasarımın görünüşü kullanıcı beğenisini geçer | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü) — kullanıcı iki bölümü de hiç görmedi, düzenler duran yetkilendirmeyle seçildi. Geri dönüş tek hamle: Faydalar `ae80fb0:src/components/sections/Benefits.tsx` · Modüller `git revert c279aa1` |
| 15 | Roller sekmeleri doğru ekranı gösterir — eşleme ayağı | ✅ Geçti | Dört sekme tıklandı: Üye → `uye-telefon.webp` 244×516 (telefon oranı) · **Antrenör → `antrenor.webp` 562×405 (tarayıcı oranı)** · Yönetim → `cockpit.webp` 562×246. `kontrol:` `role=tab` **4**, `role=tabpanel` **0** (B-015 ailesi, kanvasta kayıtlı) |
| 16 | Roller sekmeleri doğru ekranı gösterir — diyetisyen ayağı | ❌ Kaldı | Diyetisyen → `grup.webp` 562×281 ödünç görsel, alt metni görüntüde duranı anlatıyor. **TASK-3.24 ❌ İptal** (kullanıcı kararı 2026-09-25). Düzeltme task'ı AÇILMADI — kullanıcının kapattığı iş yeniden açılmaz; rota ayrı bir quick turu |
| 17 | Yazı tipi kümesindeki her karakter dosyada gerçekten bulunur | ✅ Geçti | Dal 2: **5 woff2 × 153 = 765 kesin ölçüm / 0 sonuçsuz** · muaf 10 (`₺` + dört ok, Sora 700 **ve** 800'de); Inter 400/500/600 **tam**. `kontrol:` muafiyet sitenin 6 gerçek yığın çiftine karşı **30 ölçümle** sınandı |
| 18 | Site metni yazı tipi kümesinin dışına çıkmaz | ✅ Geçti | Dal 1: 16 sayfa · **85.129 karakter** · kümede olmayan karakter yok. ⚠️ Kapsam: `display:none` ve yalnız dar kırılımda çizilen metin dışarıda, dal 1'in **kapsam tabanı yok** |
| 19 | Sayfa ağırlığı ve LCP M6 regresyon çizgisini aşmaz | ✅ Geçti | Ağırlık **iki koşumda birebir**: `/` **111 KB** iki profilde · `/fiyat` 95 · segment 125 · `/demo` 73 KB — çizgi **144 KB**, pay 33 KB. ⚠️ **LCP belirlenimli DEĞİL** ve yargı ona dayanmıyor: aynı imajda iki koşum `/` masaüstü **156 → 100 ms** okudu (makine yükü 14,94); sessiz koşum çizginin (96 ms) 4 ms üstünde. Yöntem kusuru zaten kayıtlı (B-035) |
| 20 | CLS sıfıra yakın | ✅ Geçti | 4 rota × 2 profil × **iki koşum = 16 ölçümün 16'sında da CLS 0** |
| 21 | Konsol temiz | ✅ Geçti | `/` @1440 18 kare / 15.405 px · `/` @390 20 kare / 25.872 px · `/demo` @390 6 kare / 4.451 px — üçünde de temiz, üç rakam da tabanla birebir. ⚠️ **Hedef geliştirme sunucusu (3000), `scan.mjs`'te sabit** — yayın kopyası bu senaryoda hiç ölçülmedi |
| 22 | Dekoratif dev rakamlar (404 · çöküş · `/gecis`) erişilebilirlik ağacından düşer | ✅ Geçti | CDP tam AX ağacı, 390 ve 1440: `/gecis` `01`/`02`/`03` → **0 düğüm, "ignored" olarak bile yok** · `/olmayan-sayfa` `404` → **0 düğüm** · `WhyUs`in 48 px rakamı ağaçta **yok** · `global-error.tsx:30` kaynakta `aria-hidden`. **İki negatif kontrol:** `/gecis`'in gizlenmemiş `1`/`2`/`5` rakamları **2'şer düğüm**, ve `/`de ağaçta görünen `01` düğümleri ürün turunun **11 px numaralı noktasına** ait çıktı (CDP `DOM.resolveNode` ile çözüldü), dekoratif rakama değil |
| 23 | Hareket azaltma açıkken ara opaklık kalmaz (kontrast ölçümünün ön koşulu, M2 F2.3) | ✅ Geçti | `reduce`: **geçiş-ortası opaklık 0** · `.reveal` **0** · koşan animasyon **0**. `kontrol:` `no-preference`te **41 oynayan** + 39 `.reveal` + 80 animasyon — sonda iki hâli ayırt ediyor. ⚠️ **Ölçünün okunuşu netleşti:** `reduce` altında ara opaklıklı **22 eleman** var ama **22'sinin 22'si STATİK tasarım** (her biri kareler boyunca tek değer: 0,45 · 0,5 · 0,7 · 0,55 · 0,045 · 0,04 — dekoratif katmanlar), hiçbiri geçiş ortası değil |
| 24 | Hareket azaltma açıkken çapa kaydırması animasyonsuz olur — CSS ayağı (QUALITY 7) | ✅ Geçti | 16 rota × 2 genişlik, 15.874 eleman: `reduce`ta `scroll-behavior != auto` **0**, `no-preference`ta **32** (hepsi `html`, yumuşak). Gerçek yüzey `/demo` sonuç kutusu (0 → 1.334 px): `reduce` **2 kare / 1 ara değer / ~16 ms** ↔ `no-preference` **20 kare / 18 ara değer / ~313 ms` · → TASK-3.27 |
| 25 | Demo formu 503 verdiğinde kullanıcının yazdıkları kaybolmaz | ✅ Geçti | `tests/whatsapp-draft.test.ts` **9 test** geçti; canlı ölçüm (uç `page.route` ile taklit — canlı depoya kayıt yazılmadı): `no-sink`te ad · kulüp · telefon **3/3** taşınıyor, e-posta **taşınmıyor** |
| 26 | Ön-doldurma yalnız `no-sink`'te kurulur, doğrulama hatalarında kurulmaz | ✅ Geçti | 6 dal canlı koşuldu: **yalnız `no-sink` (503)** ön-doldurma kuruyor; `missing` · `missing-contact` · `bad-contact` · `no-consent` (422) ve `rate-limited` (429) taban `href`te kalıyor |
| 27 | Ön-doldurulan kişisel verinin yasal metinde karşılığı var | ❌ Kaldı | `kanal: kullanıcı kararı` — ölçüldü: `legal.ts`te "WhatsApp" dizesi **0 kez** geçiyor, KVKK → Aktarım listesi hâlâ **dört tedarikçi** (Vercel · Hetzner · Resend · Google Workspace). Düzeltme task'ı AÇILMADI: hukuki nitelendirme hukukçunun (B-008), üç seçenek kullanıcı kararı bekliyor (`BULGULAR.md` → Gelen Kutusu `[TASK-3.25 SORU]`). Site `noindex` önizlemede — **yayını bloke etmez, yayından önce kapanmalı** |
| 28 | Batarya, tip kontrolü ve bağımlılık taraması temiz | ✅ Geçti | `npm test` **219 geçti + 2 atlandı** (iki env kapısı kapalı — arıza değil) · `tsc --noEmit` **0** · `npm audit` **0 açık** |
| 29 | Fazın yeni dönüşüm yüzeyi olay sayımıyla geldi (QUALITY 9) | ✅ Geçti | Dar kırılımdaki "Demo" bağlantısı (`Header.tsx:125`) `header` yüzeyinin alt ağacında (`Header.tsx:61` `data-surface`); atıf `ClickTracker.tsx:15` `anchor.closest("[data-surface]")` ile kuruluyor; `click-tracker.test.ts` **5 test** geçti |
| 30 | Yeniden tasarlanan iki bölüm yeni iddia yazmaz (QUALITY 10) | ✅ Geçti | `iddia-metinleri.test.ts` **46 test** · `capabilities.test.ts` **62 test** geçti; metin `src/content/`te kaldı |
| 31 | Yayın hattının tek otomatik sinyali yeşil | ✅ Geçti | Vercel `alpfitplus-web-v2`: listelenen **20 dağıtımın 20'si `● Ready`** (Production), en yenisi **10 dakika önce** = fazın son commit'i (`77ea77a`). CI yok (M6 F6.3), `.github/` mevcut değil |
| 32 | Gerçek telefonla uçtan uca tur (Faz 2'den devredilen form denemesi dâhil) | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı cihazı) — kapsam kararının kendi cümlesi: *"kullanıcının gerçek telefon turu fazın sonunda, doğrulama olarak koşar"*. Alt ajan koşamaz; kayıt Gelen Kutusu `[TASK-1.06]` |
| 33 | Onay e-postası gelen kutusuna mı spam'e mi düşüyor | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü) — Faz 1'den devredildi |
| 34 | Ekran okuyucu onay kutusunu iki kez duyuruyor mu | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü/kulağı) — projede ekran okuyucu ölçüm kanalı **yok** ve bu fazda açılmadı (kapsam dışı); kayıt Gelen Kutusu `[TASK-2.05]` |
| 35 | `DEMO_TO`'ya giden e-postanın yerleşimi | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü) — Faz 1'den devredildi |
| 36 | Umami panelinin **arayüzünde** v2 kaydı görünüyor mu | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü) — ürün tarafı Faz 1'de okuma API'siyle ölçülmüştü; panel arayüzü kullanıcıda |
| 37 | Onay kutusu 18 px görünür / 44 px hedef, hata ve odak deyimi korunur | ✅ Geçti | 320 ve 390 px'te: hedef kutusu **44×44** · görünür kutu **18×18** · yerli çizim bırakılmış · komşu bağlantıyla **çakışma 0**. Dört hâl ayrı ayrı ölçüldü: işaretsiz `rgb(211,215,202)` **1 px** halka · işaretli zemin `rgb(62,107,60)` · **geçersiz `rgb(179,66,54)` 2 px halka — işaretli hâlde de duruyor** · odak `rgb(62,107,60)` **2 px kontur, kırmızının üstünde**. Geçersiz hâlde ayrıca `aria-invalid=true` + `aria-describedby=consent-error` + hata metni. ⚠️ `forced-colors` ekseni hiçbir kapının kapsamında değil ve bu turda da ölçülmedi — kayıt Gelen Kutusu `[TASK-3.17]` |
| 38 | Lint kapısı yeni problem üretmedi | ✅ Geçti | **30 problem (25 hata + 5 uyarı)** — B-028'in kaydıyla birebir, yeni problem yok. `npm outdated` 9 paket geride, `npm audit` 0 açık |
| 39 | `/gecis`'in beş kontrast kalemi 320 · 390 · 1440 px'te AA'yı geçer (TASK-3.26'nın ürünü) | ✅ Geçti | Beş rozet rakamının beşi de **`p02` = `min` = `med` = 8,81** ve tek zemin `rgb(33,35,31)`, **üç genişlikte de birebir** (eşik 4,5 — 14px/800 büyük metin değil). Aynı turda paragraf da ölçüldü: yapışkan başlığın altından çıkarıldığında **`p02` = `min` = `med` = 7,05**, zemin saf beyaz ×6175, üç genişlikte de — yani kapının gördüğü 3,25 sayfanın değil **maskenin** kusuru (B-063), bağımsız olarak yeniden üretildi |
| 40 | Hareket azaltma açıkken JS'te `behavior` veren kaydırma çağrısı da tercihi okur (TASK-3.27'nin ikinci yüzeyi) | ✅ Geçti | Asistan akışı (taşma 236 px): `reduce` **1 kare / 0 ara değer** ↔ `no-preference` **15 kare / 14 ara değer**. Kabın kendi `scroll-behavior`ı iki hâlde de `auto`, yani yumuşaklık tümüyle JS argümanından geliyor. `ters-çevirme:` çağrı koşulsuz yumuşağa çevrilince (düzeltme öncesi hâl) `reduce` da **15 kare / 14 ara değer** verdi → geri alındı (kaynağa dokunulmadı, enjekte yalnız o bağlamda yaşadı) — sonda kör değil · → TASK-3.27 |

**Otomatik kontrol bulguları (Adım 1 — 2. tur, baştan koşuldu):**

- **1a — CI yok.** `.github/` dizini **mevcut değil** (M6 F6.3 sonraki fazda). Push sonrası tek otomatik sinyal Vercel derlemesidir: **20/20 `● Ready`**, başarısız dağıtım yok; en yenisi bu turun başlangıcından 10 dakika önce = fazın son commit'i (senaryo 31).
- **1b — Bağımlılık/kalite botu yok.** Elle ölçüldü: `npm audit` **0 açık** · `npm outdated` **9 paket geride** (kayıt Gelen Kutusu `[PHASE-1]`, aciliyet yok) · `npm run lint` **30 problem (25 hata + 5 uyarı), yeni yok** (B-028 açık).
- **1c — Güvenlik taraması, faz penceresi `6f4eca9..HEAD`.** Pencere önceki tura göre **büyüdü** (99 → **103 dosya**, +9.009/−422 → **+9.770/−429**); çapa sabit kaldığı için TASK-3.26 ve TASK-3.27 kendiliğinden içeri girdi. Mercek: injection · yetki atlaması · hardcoded secret · veri sızıntısı · hassas veri loglama. **Somut bulgu yok:** eklenen satırlarda `dangerouslySetInnerHTML` / `eval` / `new Function` / `innerHTML` / `child_process` **0 eşleşme**, sır deseni yok, yeni loglama yok. İki turun deltası (`6cbef7c..HEAD`) yalnız iki `aria-hidden`/zemin niteliği, bir CSS kuralı, bir `matchMedia` okuması ve ölçümlü yorumlardan oluşuyor. **Kişisel verinin üçüncü tarafa açılan tek yeni yüzeyi** senaryo 27'de ve zaten kayıtlı — yeni satır düşülmedi.
- **1c artefakt süpürmesi — iki invaryant, biri temiz biri kayıtlı.** (a) **TASK-3.27'nin tanıttığı invaryant** — *"JS'te açıkça kaydırma davranışı veren her çağrı tercihi kendisi okur"* — kaynakta sayıldı: `scrollIntoView` / `scrollTo` / davranış argümanı için **3 dosyada eşleşme**; `DemoForm.tsx:121` ve `:127` argüman **vermiyor** (CSS'i okur, kapsandı), `Assistant.tsx:80` veriyor ve **`matchMedia` okuyor** → **atlayan çağrı sitesi 0**. (b) `research/lib/rotalar.mjs` tüketicileri yeniden sayıldı: `a11y.mjs:50` ve `mobile-audit.mjs:94` tüketiyor, **`font-guard.mjs:59` hâlâ elle liste tutuyor** — bugün kapsam boşluğu yok, risk ileriye dönük; **kalem zaten kanvasta kayıtlı** (Gelen Kutusu `[PHASE-3]`), yeniden-koşum çift kayıt üretmesin diye yeni satır düşülmedi.
- **1 — Kanvas süpürmesi (2. tur):** bu faza dokunan kalemler yeniden tarandı. **Bir Gelen Kutusu satırı mezun edildi:** `[TASK-3.08]` — kök `CLAUDE.md`'nin ölçüm betikleri tablosunun `mobile-audit.mjs` için *eski geçme şartını yazdığı* kaydı; ölçüldü ve **artık doğru** (`CLAUDE.md:252` bugün *"yatay kaydırma, taşan eleman, dokunma hedefi (16 rota, yayın kopyasına karşı)"* + *"✓ KAPI YEŞİL — 16 sayfada 0 ve çıkış kodu 0"* yazıyor), satır silindi. `[TASK-3.03]` satırı **duruyor ama ölçümü tazelendi**: doküman hâlâ 209+2 diyor, bugünkü gerçek **219+2**. Kalan kutu satırları kendi kayıtlı evlerinde bekliyor; **B-065** (🔴 JS kapalıyken demo formu talebi kaybediyor) işaretsiz açık kritik bulgu olarak bu turda da **açık bırakıldı** — konusu M3 lead hattı davranışı, fazın JS-kapalı ekseni kapsam kararıyla keşif/kayıt ekseniydi. Atom kayıtlı, yeni satır düşülmedi.
- **Bulgu çözüm teyidi (Adım 6):** bu turda arşive giden bulgu **yok**. TASK-3.26'nın kapattığı beş kalem B-032'nin *kapsanmayan yüzey* borcuydu ve o atom önceki turda zaten arşivlenmişti; **B-063 kullanıcı kararıyla AÇIK kalır** (seçenek B, 2026-09-26) ve bu turun ölçümü onu bağımsız olarak yeniden üretti (senaryo 3 ve 39). Önceki turda teyit edilen beş atom kancası (**B-030** · **B-012** · **B-051** · **B-046** · **B-057**) bu turda da birebir doğrulandı — rakam değişmediği için kancalara dokunulmadı.

**Sondanın kendi kusurları — bu turda üçü yakalandı ve düzeltildi** (5b: kör sonda yeşili kanıt değildir): (1) **S11** büyütme eksenlerinde `zoom` çarpanı uyguluyordu, oysa eksen etiketi zaten büyütülmüş CSS penceresini yazıyor — %400 ekseni 16 rotada **yanlış kırmızı** verdi, çarpan kaldırılınca 16/16; (2) **S40**'ın ilk sondası akış kabını *"taşıyor mu"* ile arıyordu, panel yeni açıldığında henüz taşmadığı için kap bulunamadı ve sonda `window.scrollY` okuyup **0 fark** bastı — kap `overflow-y` ile bulunacak şekilde düzeltildi ve ayrıca **ters çevirme** ile kırmızı üretebildiği gösterildi; (3) **S37**'nin geçersiz dalı uç `no-sink` taklit ettiği için hiç ateşlenmiyordu (`FIELD_ERRORS`te o kod yok) — uç `no-consent` döndürecek şekilde düzeltildi. Dördüncüsü kusur değil **yanlış okuma** olarak çözüldü: `/`de AX ağacında görünen `01` düğümleri dekoratif rakama değil ürün turunun **11 px numaralı noktasına** aitmiş (CDP `DOM.resolveNode` ile çözüldü).


**Kapıların kendi kör noktaları (yeşil satırların okunma sınırı):** `a11y` ve `font-guard` yalnız **1440 px**'te koşuyor ve `font-guard` `body.innerText` okuyor → yalnız `lg:` altında çizilen metin ikisinin de dışında (Gelen Kutusu `[TASK-3.16]`) · kapı **çakışmayı** ölçmüyor, tek tek kutuları ölçüyor (STYLE-GUIDE → Dokunma hedefi) · **üst üste binme (metin çakışması)** sınıfını hiçbir kapı görmüyor (B-063 · B-064 ve bu koşumun on bir kutu satırı) · hata kutusu hiçbir kapının kapsamında değil (yalnız form 503 verdiğinde çizilir) · `scan.mjs` yayın kopyasını hiç ölçmüyor (senaryo 21) · `forced-colors` ekseni ölçülmüyor (senaryo 37).

---
