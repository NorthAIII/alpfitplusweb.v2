# Phase 3 — UAT Detayı

← PHASE-3 · uat

> `_dev/phases/PHASE-3.md` → UAT Sonuçları'nın bölme çocuğudur (verify-phase boyut kapısı, 2026-09-26; faz **hâlâ aktifken** bölündü — 38 senaryonun tablosu ve otomatik kontrol dökümü yazıldığında parent 26.084 token'a çıkarak kırmızı çizgiyi aştı). Teşhis **gerçek büyüme**; kesim dokümanın kendi `## UAT Sonuçları` sınırından geçti ve bu projede emsali var (`PHASE-2-UAT.md`). Parent'ta sonucun **snapshot'ı** durur (tarih, sayı, ölçüm yüzeyi, kalan kalemler — CLAUDE.md → Faz dokümanı hibrittir: "UAT sonuç tablosu — özet kalır"); **senaryoların tam tablosu, otomatik kontrol dökümü ve kapıların kör noktaları burada.**

---

## UAT Sonuçları

> `/devflow:verify-phase` oturumunda dolduruldu (2026-09-26). **Kol: otonom** (orkestratörlü koşum — alt ajan kullanıcıyla konuşamaz).

**Tarih:** 2026-09-26
**Toplam Senaryo:** 38 | **Geçen:** 28 | **Kalan:** 10

**Ölçüm zemini — hangi senaryo hangi hedefe ölçüldü:** `a11y` · `mobile-audit` · `font-guard` · `perf` ve bu turun UAT sondası **yayın kopyası (3100)**; `scan.mjs` **geliştirme sunucusu (3000)** — betikte sabit, `BASE` taşımıyor (senaryo 21'in kapsam notu). Batarya/tip/lint `web` konteynerinde. **3100 tazelenmedi — gerekmedi ve ölçüldü:** `lastmod` **2026-09-25T01:05:55.970Z** = `632f99e` (TASK-3.22), sonrasındaki üç commit `src/`+`public/`'e dokunmadı (`git log --name-only 632f99e..HEAD -- src public` boş); pozitif kontrol `/` → `-sm.webp` **68**, negatif kontrol `salon-genis.webp` **0**.

| # | Senaryo | Sonuç | Not |
|---|---------|-------|-----|
| 1 | Kontrast kapısı 16 rotanın hepsini gezer, boyanan gerçek rengi ölçer, ölçemediğini sayar | ✅ Geçti | 16 rota (sitemap 15 + 1) · 105 ekran adımı · 1834 eleman · piksel yöntemi · ölçülemeyen yapışkan **151** · görünmez **43** · kalan **0** |
| 2 | Kontrast kapısı eşik altında sıfır-olmayan çıkış kodu verir | ✅ Geçti | `kontrol:` bugün 6 gerçek ihlalle **çıkış 1**; ölü hedefte (`BASE=…:3457`) cümle + **çıkış 1**, yığın izi yok |
| 3 | `a11y.mjs` 16 rotada TOPLAM SORUN 0 verir | ❌ Kaldı | **6 sorun · çıkış 1**, altısı da `/gecis`: `01`/`02`/`03` **p02 1,21** · "Elle tutulan kayıtlar…" **3,25** · `1` **3,49** · `2` **4,23**. Adlandıran task yoktu → TASK-3.26 |
| 4 | B-032'nin adlandırılmış kontrast kalemleri AA'yı geçer | ✅ Geçti | 15 rotada kontrast ihlali **0**; gradyan metin **19 durak ölçüldü (taban 19) / 0 eşik altı**; `alt`sız img 0 · adsız link/buton 0 |
| 5 | Başlık hiyerarşisi 16 sayfada atlamasız, her sayfada tek `h1` | ✅ Geçti | **316 görünür başlık (taban 316) · 0 sayfada 0 atlama** · her rotada `h1:1` |
| 6 | Mobil kapı 320 ve 390 px'te kesilen metin bulmaz | ✅ Geçti | Kırpılmış taşma **0 gerçek · en ağır 0 px**, iki genişlikte; 2054 metin elemanı (taban 2054) · 6253 eleman |
| 7 | Kaydırılabilir şeritte kart kabın görünür genişliğine sığar | ✅ Geçti | Şerit ihlali **0**, iki genişlikte; 5 kaydırılabilir kap (taban 5) · iki boyutlu içerik muafiyeti 2 |
| 8 | Dönüşüme dokunan her hedef ≥ 44×44 px | ✅ Geçti | Kritik küme **305 ölçüldü / 0 eşik altı / 0 benzersiz (taban 305)**, 320 ve 390 px'te birebir |
| 9 | Gezinme kulvarı ölçülür ve raporlanır ama kapıyı düşürmez | ✅ Geçti | **333 ölçüldü / 261 eşik altı** → alt bilgi 256 · içerik yolu 4 · gövde metni 1; TOPLAM SORUN 0, çıkış 0 (kullanıcının kademeli kuralı) |
| 10 | Mobil kapı ölçemediği hâlde kırmızıya döner | ✅ Geçti | `kontrol:` ölü hedefte cümle + **çıkış 1** (yığın izi yok) ↔ 3100'de **çıkış 0**; yani yeşil, ölçümün yapıldığı hâlin yeşili |
| 11 | Telefonda her sayfanın ilk ekranında demoya çıkan bir yol var | ✅ Geçti | **9 eksende 16/16** (boş ekran 0): 320×568 · 390×844 · 412×915 · 768×1024 · %200 (640×512) · %400 (320×256) · yatay 844×390 ve 915×412 · 1440×900. `kontrol:` eksen başına **174 dönüşüm bağlantısı** tarandı (kullanılabilirlik: render + `visibility` + etkin opaklık + `pointer-events`) |
| 12 | Faydalar bölümü reddedilen kalıptan çıktı | ✅ Geçti | Üç genişlikte (1440/390/320) **ızgara kabı 0 · ikon karosu 0**; gerçek fotoğraf **1**. `kontrol:` aynı sonda `WhyUs`'ta **1 kap + 4 karo**, `SegmentsGrid`'de **1 kap** buluyor — dedektör kör değil |
| 13 | Modüller'in tırtıklı 5'li ızgarası yeniden kuruldu | ✅ Geçti | Öne çıkanlar **0 kap / 0 karo**; bölümde kalan **1 kap (5 sütun × 5 çocuk) + 5 karo** kullanıcı kararıyla duran 5'li ikon şeridinin (@390 ve @320'de karo 0 — tek sütuna geçiyor) |
| 14 | İki yeniden tasarımın görünüşü kullanıcı beğenisini geçer | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü) — kullanıcı iki bölümü de hiç görmedi, düzenler duran yetkilendirmeyle seçildi. Geri dönüş tek hamle: Faydalar `ae80fb0:src/components/sections/Benefits.tsx` · Modüller `git revert c279aa1` |
| 15 | Roller sekmeleri doğru ekranı gösterir — eşleme ayağı | ✅ Geçti | Dört sekme tıklandı: Üye → `uye-telefon.webp` 244×516 (telefon çerçevesi) · **Antrenör → `antrenor.webp` 562×405 (tarayıcı çerçevesi)** · Yönetim → `cockpit.webp` 562×246 |
| 16 | Roller sekmeleri doğru ekranı gösterir — diyetisyen ayağı | ❌ Kaldı | Diyetisyen → `grup.webp` 562×281 ödünç görsel, alt metni görüntüde duranı anlatıyor. **TASK-3.24 ❌ İptal** (kullanıcı kararı 2026-09-25; ön koşul ürün deposunda ölçüldü ve kalıcı olarak sağlanmadı). Düzeltme task'ı AÇILMADI — kullanıcının kapattığı iş yeniden açılmaz; rota ayrı bir quick turu |
| 17 | Yazı tipi kümesindeki her karakter dosyada gerçekten bulunur | ✅ Geçti | Dal 2: **5 woff2 × 153 = 765 kesin ölçüm / 0 sonuçsuz** · `woff2'de eksik: 0 (muaf: 10)`; Inter 400/500/600 **tam**, muaf 10 = `₺` + dört ok, Sora 700 **ve** 800'de. `kontrol:` muafiyet sitenin 6 gerçek yığın çiftine karşı 30 ölçümle sınandı, hepsi indirilmiş bir yüzle çiziliyor |
| 18 | Site metni yazı tipi kümesinin dışına çıkmaz | ✅ Geçti | Dal 1: 16 sayfa · **85.129 karakter** · kümede olmayan karakter yok. ⚠️ Kapsam: `display:none` ve yalnız `lg:` altında çizilen metin dışarıda, dal 1'in **kapsam tabanı yok** (bilinçle M6 fazına bırakıldı) |
| 19 | Sayfa ağırlığı ve LCP M6 regresyon çizgisini aşmaz | ✅ Geçti | `/` **111 KB** iki profilde (LCP 80 ms masaüstü / 72 ms mobil) · `/fiyat` 95 · segment 125 · `/demo` 73 KB. Çizgi **144 KB / 96 ms** — pay 33 KB |
| 20 | CLS sıfıra yakın | ✅ Geçti | Dört rota × iki profil = 8 kombinin **8'inde de CLS 0** |
| 21 | Konsol temiz | ✅ Geçti | `/` @1440 18 kare / 15.405 px · `/` @390 20 kare / 25.872 px · `/demo` @390 6 kare / 4.451 px — üçünde de temiz. ⚠️ **Hedef geliştirme sunucusu (3000), `scan.mjs:14`'te sabit** — yayın kopyası bu senaryoda hiç ölçülmedi |
| 22 | 404 ve çöküş sayfasının dev rakamı dekoratif sayılır | ✅ Geçti | `/olmayan-sayfa` "404" 112px **aria-hidden=true** · `/` WhyUs 01-04 48px **aria-hidden=true** · `global-error.tsx` kaynakta 2 `aria-hidden`; a11y kapısı ikisinde de 0 ihlal |
| 23 | Hareket azaltma açıkken ara opaklık kalmaz (kontrast ölçümünün ön koşulu, M2 F2.3) | ✅ Geçti | `reduce`: `.reveal` **0** · ara opaklık **0** · koşan animasyon **0**. `kontrol:` `no-preference`: 39 reveal / **39 ara opaklık** / 5 animasyon — sonda iki hâli ayırt ediyor |
| 24 | Hareket azaltma açıkken çapa kaydırması animasyonsuz olur (QUALITY 7) | ❌ Kaldı | `html scroll-behavior` **iki tercihte de `smooth`** — `globals.css:326-332` `reduce` bloğu yalnız `animation-*` ve `transition-duration` sıfırlıyor, `:203`'teki `scroll-behavior: smooth` yerinde kalıyor → TASK-3.27 |
| 25 | Demo formu 503 verdiğinde kullanıcının yazdıkları kaybolmaz | ✅ Geçti | `whatsappDraftHref` ad · kulüp · telefon taşır, `encodeURIComponent`, alan başına 120 karakter tavanı; `tests/whatsapp-draft.test.ts` **9 test** geçti |
| 26 | Ön-doldurma yalnız `no-sink`'te kurulur, doğrulama hatalarında kurulmaz | ✅ Geçti | Kaynak + batarya: `missing` · `missing-contact` · `bad-contact` · `no-consent` · `rate-limited` dallarında taban `href`; her gönderimde sıfırlanıyor |
| 27 | Ön-doldurulan kişisel verinin yasal metinde karşılığı var | ❌ Kaldı | `kanal: kullanıcı kararı` — KVKK → Aktarım **dört tedarikçi** sayıyor (Vercel · Hetzner · Resend · Google Workspace), WhatsApp yok; üstündeki cümle koşulsuz olumsuz. Düzeltme task'ı AÇILMADI: hukuki nitelendirme hukukçunun (B-008) ve üç seçenek kullanıcı kararı bekliyor (`BULGULAR.md` → Gelen Kutusu `[TASK-3.25 SORU]`). Site `noindex` önizlemede — **yayını bloke etmez, yayından önce kapanmalı** |
| 28 | Batarya, tip kontrolü ve bağımlılık taraması temiz | ✅ Geçti | `npm test` **219 geçti + 2 atlandı** (iki env kapısı kapalı — arıza değil) · `tsc --noEmit` **0** · `npm audit` **0 açık** |
| 29 | Fazın yeni dönüşüm yüzeyi olay sayımıyla geldi (QUALITY 9) | ✅ Geçti | Header'ın `lg:` altındaki "Demo" bağlantısı `header` yüzeyine atfediliyor — `ClickTracker.tsx:15` `anchor.closest("[data-surface]")`, `Header.tsx:61` `data-surface={SURFACES.header}`; `click-tracker.test.ts` 5 test geçti |
| 30 | Yeniden tasarlanan iki bölüm yeni iddia yazmaz (QUALITY 10) | ✅ Geçti | `iddia-metinleri.test.ts` **46 test** · `capabilities.test.ts` **62 test** geçti; metin `src/content/`'te kaldı, bileşene taşınmadı |
| 31 | Yayın hattının tek otomatik sinyali yeşil | ✅ Geçti | Vercel `alpfitplus-web-v2`: listelenen **20 dağıtımın 20'si `● Ready`** (Production), en yenisi 21 saat önce = fazın son commit'i. CI yok (M6 F6.3), `.github/workflows/` mevcut değil |
| 32 | Gerçek telefonla uçtan uca tur (Faz 2'den devredilen form denemesi dâhil) | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı cihazı) — kapsam kararının kendi cümlesi: *"kullanıcının gerçek telefon turu fazın sonunda, doğrulama olarak koşar"*. Alt ajan koşamaz; kayıt `BULGULAR.md` → Gelen Kutusu `[TASK-1.06]` |
| 33 | Onay e-postası gelen kutusuna mı spam'e mi düşüyor | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü) — Faz 1'den devredildi |
| 34 | Ekran okuyucu onay kutusunu iki kez duyuruyor mu | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü/kulağı) — projede ekran okuyucu ölçüm kanalı **yok** ve bu fazda açılmadı (kapsam dışı); kayıt Gelen Kutusu `[TASK-2.05]` |
| 35 | `DEMO_TO`'ya giden e-postanın yerleşimi | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü) — Faz 1'den devredildi |
| 36 | Umami panelinin **arayüzünde** v2 kaydı görünüyor mu | ❌ doğrulanamadı | `kanal: UAT` (kullanıcı gözü) — ürün tarafı Faz 1'de okuma API'siyle ölçülmüştü; panel arayüzü kullanıcıda |
| 37 | Onay kutusu 18 px görünür / 44 px hedef, hata ve odak deyimi korunur | ✅ Geçti | Kritik hedef kümesinde 0 eşik altı (kutu artık ölçülüyor); çakışma düzeltmesi yerinde. ⚠️ `forced-colors` ekseni **hiçbir kapının kapsamında değil** ve bu turda da ölçülmedi — kayıt Gelen Kutusu `[TASK-3.17]` |
| 38 | Lint kapısı yeni problem üretmedi | ✅ Geçti | **30 problem (25 hata + 5 uyarı)** — B-028'in kaydıyla birebir, yeni problem yok. `npm outdated` 9 paket geride (`npm audit` 0 açık) |

**Otomatik kontrol bulguları (Adım 1):**

- **1a — CI yok.** `.github/workflows/` mevcut değil (M6 F6.3 sonraki fazda). Push sonrası tek otomatik sinyal Vercel derlemesidir: **20/20 `● Ready`**, başarısız dağıtım yok (senaryo 31).
- **1b — Bağımlılık/kalite botu yok.** Elle ölçüldü: `npm audit` **0 açık** · `npm outdated` **9 paket geride** (kayıt Gelen Kutusu `[PHASE-1]`, aciliyet yok) · `npm run lint` **30 problem, yeni yok** (B-028 açık: Next 16 ESLint entegrasyonunu kaldırmış, lint'e ulaşan otomatik yol yok).
- **1c — Güvenlik taraması, faz penceresi `6f4eca9..HEAD`** (99 dosya, +9.009/−422). Mercek: injection · yetki atlaması · hardcoded secret · veri sızıntısı · hassas veri loglama. **Somut bulgu yok:** eklenen satırlarda sır deseni yok, `dangerouslySetInnerHTML`/`eval`/`innerHTML` yok, yeni loglama yok. `whatsappDraftHref` (`site.ts`) `encodeURIComponent` kullanıyor, alan kümesi üç kalemle sınırlı (serbest mesaj ve e-posta **bilinçle** dışarıda), tavan 120 karakter. **Kişisel verinin üçüncü tarafa açılan tek yeni yüzeyi** senaryo 27'de ve zaten kayıtlı — yeni satır düşülmedi.
- **1c artefakt süpürmesi — bir kalem çıktı.** Faz yeni bir ortak kaynak tanıttı: `research/lib/rotalar.mjs` (sitemap'ten türeyen rota listesi + `BEKLENEN_ROTA=16` tabanı). Tüketicileri `a11y.mjs` ve `mobile-audit.mjs`; **`font-guard.mjs:59` onu atlıyor ve rota listesini elle tutuyor** (16 kalem). Bugün kapsam boşluğu **yok** — elle liste sitemap'in 15'i + `/olmayan-sayfa` ile birebir örtüşüyor (ölçüldü). Risk ileriye dönük: yeni bir sayfa eklendiğinde iki kapı kendiliğinden büyür, `font-guard` dal 1 sessizce 16'da kalır ve dal 1'in **kapsam tabanı da yok**. Süzen bir kayıt bulunamadı — kapsam kararı `font-guard`'ın *çıkış kodu ve kapsam eşiklerini* erteliyor, B-012'nin kalan ayakları *`perf` ve `scan`* rota listelerini sayıyor; `font-guard`'ın rota kaynağı ikisinde de geçmiyor. Dal 1'in eşiklenmesiyle aynı sınıf olduğu için **kapsam-dışı** sayıldı ve kanvasa düştü (`BULGULAR.md` → Gelen Kutusu `[PHASE-3]`), faz bunun için bekletilmedi.
- **1 — Kanvas süpürmesi:** bu faza dokunan kalemler tarandı; **altı Gelen Kutusu satırı ölçülerek mezun edildi** (aşağıda, Adım 1 dökümü faz kapanış raporunda), kalanlar kendi kayıtlı evlerinde bekliyor. **B-065** (🔴 JS kapalıyken demo formu talebi kaybediyor, kişisel veriyi adres çubuğuna yazıyor) işaretsiz açık bir kritik bulgudur ve bu turda **açık bırakıldı**: konusu M3 lead hattı davranışı, fazın JS-kapalı ekseni kapsam kararıyla **keşif/kayıt** ekseniydi (düzeltme değil). Atom zaten kayıtlı, yeni satır düşülmedi.

**Kapıların kendi kör noktaları (yeşil satırların okunma sınırı):** `a11y` ve `font-guard` yalnız **1440 px**'te koşuyor ve `font-guard` `body.innerText` okuyor → yalnız `lg:` altında çizilen metin ikisinin de dışında (Gelen Kutusu `[TASK-3.16]`) · kapı **çakışmayı** ölçmüyor, tek tek kutuları ölçüyor (STYLE-GUIDE → Dokunma hedefi) · **üst üste binme (metin çakışması)** sınıfını hiçbir kapı görmüyor (B-063 · B-064 ve bu koşumun on bir kutu satırı) · hata kutusu hiçbir kapının kapsamında değil (yalnız form 503 verdiğinde çizilir) · `scan.mjs` yayın kopyasını hiç ölçmüyor (senaryo 21) · `forced-colors` ekseni ölçülmüyor (senaryo 37).

---
