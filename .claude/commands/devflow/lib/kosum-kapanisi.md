# DevFlow — Koşumu Kapat

> `/devflow:run-phase`'in **Adım 7**'si: koşum bittiğinde **bir kez** koşan kapanış. **Tek çağıranı
> odur**; ayrı dosya olmasının nedeni çağıranın tek-Read sınırıdır (kanon: Doküman Disiplini →
> Boyut ve Bölünme; ölçüt: taşınan birimin ve her çağıranın tek-Read'e sığması). Aşağıdaki `📋`
> listesi **`run-phase.md`'in durma ölçütlerine numarayla** bağlıdır (`ölçüt N`). Bu dosyaya `Adım 7` diye atıf yapan evleri sayma, ölç: `grep -rn 'Adım 7' commands/devflow/run-phase.md commands/devflow/lib/` — bu reçete bilinçle **dar**: aynı dize motor genelinde başka komutların kendi 7. adımıdır, genişletme; ama kapsam içinde de gürültü vardır, dönen her satırın gerçekten **bu dosyaya** atıf yaptığını doğrula. **Ters yön ise dar değildir** ve bu aile ile sınırlı da değildir; onu da sayma, ölç: `grep -rniE 'ölçüt [*_ ]*[0-9]+' commands/devflow/` — **işaretleme toleransı şart** (`ölçüt **1**'in` biçimi çıplak desenle kaçar) ve `-i` yalnız **kasayı** katlar, aksanı değil: ASCII yazım bugün motorda yok, doğarsa desen genişletilir. Dönen her satırın gerçekten `run-phase.md`'nin listesine baktığını doğrula. Ölçüt eklenir,
> silinir ya da yeniden numaralanırsa bu liste **aynı turda** düzeltilir: her ölçütün burada tam
> bir kez karşılığı olmalı, fazlası da eksiği de kusurdur.


Koşum bittiğinde **önce toplamı ölç** — `git log --oneline <koşum çapası>..HEAD` · `git diff --stat <koşum çapası>..HEAD` — sonra tek bir kapanış raporu yaz: kaç tur koştu · hangi adımlar · hangi durma ölçütüyle durdu · toplam ne değişti · **koşum boyunca bu rapora yollanan her kalem** — turların döndürdüğü kapsam-dışı kalemler · Adım 1a'nın taradığı bekleyen quick kayıtları · ertelenen ya da kalan kulvar-dışı iş (örnektir, tüketici değildir; tekrarlamadan, birleştirilmiş). Sonra kapanış bloğunu yaz. **Biçim burada sabittir — koşumun kendi kapanış şablonu budur**, tıpkı her komutun kendi şablonunu taşıması gibi; bağlamındaki proje kanonunun diyagramına göre yazma, o kopya motorun bugünkü hâlinden geride olabilir. Hükümler kanondadır (CLAUDE.md → Oturum Kapanışı):

```
<✅|⚠️|⏸️> [koşumun sonucu — tek cümle]
📋 Sıradaki adım: [ateşleyen ölçütün verdiği komut: /devflow:<komut> | ölçüt komut vermiyorsa: yok — <bekleme koşulu>]
   → [gerekçe — tek satır; `yok` dalında yazılmaz]
<⚠️|💡|✅> Açık kalemler: [önek: kalem] | yok
```


- `📋` **ateşleyen durma ölçütüne göre** seçilir (numaralar → Durma Ölçütleri). ⚠️ **DURUM'un `Adım`'ından komut türeten her kalem (1 · 12) önce Kulvar'ın ayıklamasını uygular** — kuyruk değerin hükmünü çürütüyorsa oradan komut türetilmez, `yok — <bekleme koşulu>` yazılır (koşul: `Adım` alanının kuyruğu değerini çürütüyor):
  - ⚠️ **Aynı iki kalemin (1 · 12) türetmesi KOŞULLUDUR ve koşul ölçüt numarasına değil KONUMA bakar.** Kanon aynı hükmü koşullu yazar (CLAUDE.md → Oturum Kapanışı: *"kapanış bloğu yazan ve faz döngüsünün konumunu değiştirmeyen her oturum"* aynı taramayı yapar) ⇒ **bu koşum faz döngüsünün KONUMUNU hiç değiştirmediyse** — ölçüsü alttaki maddededir ve **alan adı burada yazılmaz**, yoksa ölçü tek alana daralır — `Adım`'dan türeyen çıktı, **`yok` çıktısı dahil**, yalnız **varsayılandır**: bekleyen ⬜ ya da 🔄 bir quick kaydı varsa `📋` **o kaydın** komutudur (`/devflow:quick QUICK-NNN`), faz döngüsünün komutu `→` satırında *"ondan sonra"* diye anılır ve kayıt son satırda tekrarlanmaz (kanon: Terfi kuralı). Konum **değiştiyse** türetme koşulsuzdur ve kayıt son satıra iner — **öneki ①'in ölçütüdür**, keyfî değil: kayıt sıradaki adımı engelliyorsa `engel:`, engellemiyorsa `önerilir:`. **Kapsam bu ikisidir, liste geneli değil:** öteki kalemlerin `yok`u gerçek bir bekleme koşulu taşır ve 13'ün dal komutu (`kickoff-docs` gibi) engelin kendisidir — kanonun Terfi kuralı onları `📋`'de tutar. **1'in `prd_review_bekliyor` kolunda ise o kolun kendi ⚠️'si önce gelir:** orada iki aday çakışır ve kanonun hükmü **sormaktır**.
  - **Kapının birinci girdisi — KONUM; ölç, turu sayma, ve konumu tek alana bağlama.** `Adım` konumun yalnız bir bileşenidir: `run-task` fazda bekleyen task varken `Adım`'ı bilinçle `task`'ta bırakır (→ `run-task` → *Adım alanını ilerlet*), yani faz döngüsü ilerlerken de o alan sabit kalır — kapıyı `Adım`'a bağlamak onu her task turunda yanlış ateşletir. Ölçü **brief madde 4'ün üç alanıdır** (`Adım` · `Aktif Task` · Task Durumu tablosunun satır durumları) ve üçü de zaten Adım 3'ün her turda okuduğu alanlardır: **üçünden biri bile bu koşumun turlarına atfedilebilir biçimde değiştiyse konum değişmiştir** ve kapı ateşlemez.
    ⚠️ **Bu ölçü KULVAR-PARAMETRİK DEĞİLDİR ve yapılmamalıdır — nesnesi koşumun kendi ilerlemesi değil, FAZ DÖNGÜSÜNÜN konumudur.** FAZ dışı bir kulvarın **kendi komutu** (`quick`) o üç alana tanım gereği dokunmaz (`quick` Adım 4), yani kanonun koşulu (*"faz döngüsünün konumunu değiştirmeyen her oturum"*) orada **olağan hâlde** sağlanır ve kapının ateşlemesi **olağan** davranıştır (bekleyen bir kayıt varken kullanıcıyı faz komutuna yollamamak zaten kapının varlık sebebidir); Yetkilendirme kipinin açtığı bir faz turu o alanlara dokunursa onu yukarıdaki ölçü görür. Ölçüyü kulvarın kendi ilerleme kaynağına bağlamak kapıyı **tersine çevirir** — iş yapmış bir koşum *"konum değişti"* sayılır ve kuyruk dolu dururken faz komutu yazılır. ⚠️ **Karıştırma:** kaydın `**Durum:**` alanı kulvarın **ilerleme** ölçüsüdür (ölçüt 7 · Adım 6'nın kapısı), bu kapının değil. *"Koşum hiç tur açmadı"* da ölçütün vekilidir, kendisi değil — yetkilendirilmiş bir kulvar-dışı denetim turu tanım gereği DURUM'a dokunmaz (ölçüt 7 aynı muafiyeti adıyla veriyor), yani tur koşmuş ve konum değişmemiş olabilir.
  - **Konumun ölçüm yolu ve ölçülemediği hâl.** Açılış değerleri `git show <koşum çapası>:_dev/DURUM.md` ile alınır (aynı disiplin: brief madde 9 — *"önce" değerini `git show <çapa>:<dosya>` ile al*); fark yalnız Adım 3'ün paralel ilan ettiği ya da hiçbir turun penceresine düşmeyen commit'lerdeyse konumu bu koşum değiştirmemiştir ve kapı **ateşler** — *"ateşlemez"* hükmü yalnız aşağıdaki ÖLÇÜLEMEDİ kolunundur. ⚠️ **Çapa 1c'de alınır, kapının hedeflediği duruşların bir kısmı ondan öncedir** (1a'nın kapıları): orada çapa yoktur ve **aranmaz** — hiç tur açılmadığı için açılış değeri HEAD'inkidir. Çapa varken `git show` sıfırdan farklı çıkış verirse sonuç **ÖLÇÜLEMEDİ**'dir, *"değişmedi"* değil: kapı ateşlemez ve ölçülemediği kapanış raporunda anılır.
  - **Kapının ikinci girdisi — KAYIT LİSTESİ.** 1a'nın taramasından gelir ve koşum boyunca sabittir, yeniden tarama yapma. ⚠️ **Donmuş listeden yalnız aşağıdaki hâllerde ayrılınır ve hiçbiri yeniden tarama DEĞİLDİR.**
    **(i)** Listedeki bir kayıt koşum sırasında kapanmış olabilir. **QUICK**'te ölçü kaydın canlı `**Durum:**` değeridir — Adım 3'ün dağıtılan kayıt okuması ya da dağıtım-öncesi canlılık `grep`'i (`lib/kosum-kulvar.md` → `Kulvar — QUICK` → Sabitlik): ✅/❌ gösterdiyse ya da kayıt artık yoksa, kapatan bu koşumun turu da olsa paralel bir oturum da olsa kayıt listeden **düşer**. **BULGULAR**'da bir tur, dağıttığın bulgunun işini taşıyan bir quick kaydını devralıp kapatmış olabilir (`quick.md` Adım 1 → `B-NNN` dalı) — turun **kendi beyanı** (dönüş sözleşmesi 9a) bir kaydı kapattığını söylüyorsa o kayıt listeden **düşer**. **FAZ**'da da ölçü turun kendi beyanıdır — orkestratör o kulvarda kaydı okumaz (Adım 3): bir tur bir kaydı — (iii)'ün eklediği dahil — kapattığını söylüyorsa kayıt listeden **düşer**. Beyansız kapanışı (paralel oturum · arkadaki koşum) beyan göstermez: kapanışta listede kalan her kayda — (iii)'ün eklediği dahil, son beyanı açık dese de — 1a'nın ölçütüyle bir kez `grep` at — ✅/❌ gösterdiyse ya da kayıt artık yoksa düşer; üyelik yine donmuştur. Atlanırsa blok, bu koşumun az önce kapattığı kayda yollar.
    **(ii)** **koşum 1a'nın tarama paragrafına varmadan durduysa** (KICKOFF-NOTES · kulvar · `Adım` boş kapıları ondan öncedir) liste hiç alınmamıştır: kanonun emrettiği tek `grep` o hâlde burada atılır — **1a'nın ölçütüyle**, yani ⏸️ kayıtlar da dahil, çünkü o duruşta kapanış raporunun *"1a'nın taradığı bekleyen quick kayıtları"* kalemi de aynı taramadan beslenir ve tarama hiç koşmazsa blok kuyruğu dolu bir depoda `Açık kalemler: yok` der. `📋` kapısına yalnız ⬜/🔄 girer; ⏸️ olanın hükmü Duraklatma Notu üzerindendir. Bağlam disiplini'nin *"lib'lerin orkestratöre adıyla emrettiği hedefli okuma"* ölçütü bu `grep`'i kapsar.
    **(iii)** Turun kendi beyanı (dönüş sözleşmesi 9a) bir kayıt **AÇTIĞINI** ve ⬜/🔄 bıraktığını söylüyorsa o kayıt kapının girdisine **EKLENİR** — emrettiğin notsuz `pause`'un ⏸️ bıraktığı kayıt da (Duraklatma Notu onu taşımaz, ⏸️ dışlaması ona uygulanmaz) — ölçü yine beyandır, yeniden tarama değil, ve eklenen kaydın hükmü onu anan **son** beyandır: sonraki bir tur onu kapattığını söylüyorsa kayıt düşer; kanonun döngü-dışı varsayılanı bu koşumun hazırladığı işi de seçer.
  - ⚠️ **Kapı, ölçüt 2'nin açılış muafiyetiyle çelişmez** — aynı kayıt, iki farklı an: orası koşum **sürerken** o kaydın turunu açtırmaz (kayıt kulvarın işi değildir), bu kapı koşum **kapanırken** kullanıcıya sıradaki adımı yazar.
  - **Birden çok aday varsa sıra kuyruğa değil ENGELLEYİCİLİĞE bakar** (kullanıcı kararı, 2026-09-07): ① sıradaki adımı **engelleyen** iş, hangi kuyruktan gelirse gelsin önce gelir; ② engelleyenler kendi aralarında **kaydın kendi öncelik hükmüne** göre sıralanır — aday bir ⬜/🔄 kayıtsa kanon `## Ne Yapılacak`'ının hedefli okunmasını emreder ve bu satır o okumayı adıyla emrettiği için Bağlam disiplini'nin ölçütü onu kapsar — hüküm yoksa **sor**; ③ engelleyen yoksa devralınacak yarım ya da hazırlanmış iş faz döngüsünün önüne geçer — bunu **üstteki kapı** yapar; ④ **①–③'ten `📋`'ye çıkmayan** kalemler son satıra iner: engelleyenler `engel:`, ötekiler `önerilir:` önekiyle, ve `önerilir:` olanlar **kuyruklar arası yeniden sıralanmaz** — her kuyruğun kendi sırası geçerlidir: **BULGULAR'da index'in kendi sırası** (önem işareti rapora girer, sıraya değil — iki ölçü ayrışırsa hakem index konumudur; ev: `lib/kosum-kulvar.md` → `Kulvar — BULGULAR` → Sıra) · quick kaydının öncelik hükmü. ①–② kanonun Terfi kuralının tek adımdan **kuyruğa** genişletilmiş hâlidir; ③ terfi değil **varsayılanın seçimidir** ve kanon ikisini adıyla ayırır. İkisinde de yeni mekanizma yok. ⚠️ **Orkestratör "kolay"ı yargılamaz** — o eksen motorda tanımlı değildir ve ölçülemez; ölçebildiği tek şey engelleyicilik beyanıdır.
  - **1** — DURUM'un yeni **Adım**'ının komutu; **tanınmayan bir değer taşıyorsa** `yok — [bekleme koşulu]` (koşul: DURUM'un Adım değeri tanınmıyor). **Adım boşsa önce Versiyon Sonu Durumu'na bak; iki kol vardır:**
    - **`prd_review_bekliyor` → `/devflow:prd-review`.** Kanon bu hâlde komutu adıyla verir ve `templates/DURUM.md`'nin gloss'u ile `prd-review`'un kendi state açıklaması onu doğrular; yazdığın şey bir **öneridir**, dağıtım değil — `prd-review` kara listededir ve bu koşum onu hiçbir hâlde açamaz.
      ⚠️ **Koşulsuz değildir — kayıt listesine bak** (üstteki ikinci girdi, (i)–(iii) uygulanmış). Bekleyen bir ⬜/🔄 kayıt kaldıysa `prd-review`'u tek başına yazma: kanonun Terfi kuralı öncelik hükmü taşıyan kaydı öne alır ve *"hangisinin önce geldiği belli değilse bu bir sorudur"* der — **bloğu yazmadan sor.** Bunun en sık hâli beyanlı yayın hattıdır: `lib/yayin-boslugu.md` prd-review'u koşulsuz önermez, **ölçüme** bağlar (*"sıradaki adım yayındır, prd-review ondan sonra gelir"*) ve ölçümünü ⬜ bir kayda yazar — yani o dosya bu kolun tanığı değil, koşuludur.
      **Turun bloğu varsa ve başka bir komut gösteriyorsa** hüküm bloğun taşıdığı komuta göre ayrılır: komut **kulvar-dışı ise terfidir, çelişki değil** — sıra listenin kendi kapanış hükmünündür (kanonun Terfi kuralı; kanon terfi etmiş komutu bu çelişkinin dışında tutar). Kulvar-içi bir komutsa çelişkidir: dağıtma, ikisini de raporla.
    - **Başka her değerde hüküm turun kendi bloğunundur** — orada sıradaki iş yayın boşluğunun ölçümüne bağlı olabilir ve o ölçümü yalnız kapanan tur yapmıştır; **blok yoksa** `yok — [bekleme koşulu]` (koşul: versiyon-sonu hâli ölçülemedi). Kendi başına türetme: kanonun bu kolu **komut yazdırmaz** (versiyon-geçişi sınırı) ve yayın boşluğunu da ölçmez.
  - **2** — terfi eden komut; DevFlow-dışı engelde `yok — [bekleme koşulu]`. ⚠️ **BULGULAR kulvarında** `📋` sıradaki işin kaynağı olarak okunmaz, ama bu kalem onu okur: ölçüt 2 `→` iziyle ateşlediyse `📋` turun bloğunun `📋`'sidir — terfi eden iş yalnız orada durur (kanon: terfi eden iş son satırda tekrarlanmaz); `engel:` iziyle ateşlediyse o kalemin komutu, komut taşımıyorsa `yok — <kalemin bekleme koşulu>`; iki iz birlikteyse `→`'nınki geçerlidir — kanon ilk engelleyeni `📋`'ye, kalanları `engel:`'e yazar (Terfi kuralı).
  - **11** — `/devflow:run-phase`; koşum sürdürülebilir hâlde durdu. ⚠️ **Koşum FAZ dışı bir kulvardaysa komut, AKTİF KULVARIN işaretiyle birlikte yazılır:** `/devflow:run-phase KULVAR: <aktif kulvarın işareti>` (bugün `quick` · `bulgular`; sözlüğün evi `lib/kosum-zemini.md` → 1a). İşareti `→` satırına bırakma — kullanıcı `📋`'deki **komutu** kopyalar, gerekçeyi değil; işaretsiz kopyalanan komut sonraki koşumu **FAZ kulvarında** açar ve bu kulvarlar tanımı gereği arkada bir faz koşumu koşarken kullanıldığı için aynı depoda ikinci bir faz dağıtımı doğurur. (İşaret zaten *"talimatın ilk satırı"*dır; komutun hemen ardına tek satırda yazıldığında da ilk satırdır — yeni bir biçim değildir.) **İstisnası ölçüt 11'in uçuşta-ateşleme fıkrasıdır** (sahipsiz yarım tur kaldıysa `yok — <bekleme koşulu>`; notu yazdırılmayan bir `pause` ⏸️ kayıt bıraktıysa `/devflow:quick QUICK-NNN`, koşumun komutu `→` satırında *"ondan sonra"*).
  - **12** — sınırı **kullanıcı** çizdi, koşum tükenmedi: `📋` ölçüt **1'in kuralıyla** yazılır — **listenin giriş kapısı dahil**, yani `Adım`'dan türetme burada da koşulludur. `/devflow:run-phase` **yazılmaz** — talimatsız açılan sonraki koşum, sınırın bilerek dışarıda bıraktığı adımı Adım 1d'nin önden-cevaplarıyla koşar; `verify`'da bu, kullanıcının kendine sakladığı kabul testinin **otonom** koşması demektir.
    ⚠️ **Bu yasak FAZ kulvarınındır — gerekçesiyle birlikte oku, kulvarla birlikte düşer.** FAZ dışı bir kulvarda dağıtılan komut kümesi tektir (`quick`) ve sınırın dışarıda bıraktığı bir faz adımı **yoktur**; üstelik giriş kapısı orada **olağan hâlde** ateşler (üstteki ⚠️), yani ölçüt 1'in kuralı `📋`'ye bekleyen bir quick kaydını ya da faz komutunu yazar ve koşumu **kendi kuyruğundan koparır**. O kulvarlarda `📋`, kalem 11'in biçimiyle yazılır (`/devflow:run-phase KULVAR: <aktif kulvarın işareti>`) ve kalan kuyruk `→` satırında anılır.
  - **4 · 5 · 6 · 7 · 8 · 9 · 10 · 14** — `yok — [bekleme koşulu]`; bekleme koşulu ölçütün kendisidir (14'te: tablonun dağıtılabilir hâle gelmesi). **7 buradadır**, sürdürülebilir kovada değil: hiçbir şey üretmemiş bir koşuma geri göndermek kullanıcıyı aynı duvara çarptırır, önce teşhis gerekir. 10'da kullanıcının müdahalesi bir komut işaret ediyorsa o yazılır.
  - **13** — dalın kendi bildirdiği komut varsa o (`kickoff-docs` gibi), yoksa `yok — [bekleme koşulu]`.
  - **3** koşumu bitirmez (Adım 5'e döner) ve bu listede yer almaz.
  İki ölçüt birlikte ateşlediyse sıra **kanonun Terfi kuralınındır**.
- Son satır, turların döndürdüğü `engel:` / `önerilir:` kalemlerinin **ve bu oturumun kendi kararıyla ertelediği / dağıtmadığı kulvar-dışı işin** birleşimidir (Yer kısıtının ertelediği tur · ölçüt 2'nin dağıtmadığı kuyruk) — yoksa blok, nesir raporda adıyla anılan bir işin üstüne `✅` yakar. **Kalemlerin sırası yukarıdaki engelleyicilik kuralının ④'üdür:** `📋`'ye terfi etmeyen `engel:` kalemleri önce yazılır — satır onları taşır (DevFlow-dışı engel · terfi edenden arta kalan engelleyenler) ve amblem kanonun hesabıyla onların varlığına göre yanar — `önerilir:` kalemleri sonra ve kuyruklar arası yeniden sıralanmadan. **Birleştirme biçimi kanondadır** (Oturum Kapanışı → satırın biçimi): kalemler ` · ` ile ayrılır ve kalemin kendi metni o ayracı içeremez — koşum kapanışı bu satırın motorda en çok kalem taşıyan hâlidir, sınırı burada kaybetmek N turun kalemlerini tek kaleme çevirir. Terfi eden iş iki satırda tekrarlanmaz (kanon).
- **Alt ajanların blokları bu oturumun nihai bloğu değildir** — onlar ara çıktıdır; son söz bu bloktur.

Kalıcı bir kayıt gerekiyorsa **önce evini ayır** (kanon: CLAUDE.md → Oturum Kapanışı → Ön-hazırlık ve onun "Sınır — kanvasla karıştırma" fıkrası): kararı verilmiş, yalnız zamanı sonraya bırakılmış iş → ⬜ QUICK kaydı (dört koşul) · görülen ama ne yapılacağı henüz kararlaşmamış sorun → BULGULAR Gelen Kutusu satırı. Kaydı **ihtiyacı gördüğün turun dönüşünde** yerleştir: sıradaki tur açılıyorsa kalem **onun** brief'ine girer (→ brief madde 10) ve o ajan kendi commit'inden önce yazar. Sıradaki tur açılmıyorsa (durma ölçütü ateşledi) ya da yazacak ajan kalmadıysa (ölüm · çelişki · ÖLÇÜLEMEDİ) **kayıt bu koşumda açılmaz:** iş son satırda kimliksiz komutla tarif edilir (`önerilir: <iş> — /devflow:quick ile`) ve kaydı işi alan oturum açar. Bu oturum repoya yazmaz.

